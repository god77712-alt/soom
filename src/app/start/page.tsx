import { redirect } from "next/navigation";

/**
 * `/start` → `/` 로 보낸다 (2026-08-22).
 *
 * ── 왜 없앴나 ────────────────────────────────────────────
 * 현관이 소재 목록으로 바뀌면서 이 화면과 **같은 것을 두 곳에서 그리게 됐다.**
 * 내비에도 「소재로 찾기」가 따로 있어서, 로고를 눌러도 그 링크를 눌러도
 * 사실상 같은 화면이 나왔다.
 *
 * 같은 화면이 두 개면 쓰는 사람은 **둘이 어떻게 다른지부터 알아내야 한다.**
 * 그게 서비스를 어렵게 만든다. 검색 결과와 기존 링크를 살리려고 경로만 남기고
 * 내용은 지웠다.
 *
 * ⚠️ 되살리지 말 것. 소재 목록을 손볼 일이 생기면 `SubjectEntry` 를 고친다.
 *    두 벌이 되면 반드시 어긋난다 — 이 저장소가 검색어 표·matcher·판정 함수에서
 *    이미 세 번 겪었다.
 */
export default function StartPage() {
  redirect("/");
}
