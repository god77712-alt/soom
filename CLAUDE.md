# CLAUDE.md — 숨(SOOM) 프로젝트

## 프로젝트 정체성

크리에이터에게 **"이 소재가 반응이 좋고, 여기서도 찍을 수 있다"를 데이터로 보여주는**
촬영지 서비스. 2026 관광데이터 활용 공모전 출품작.

> ⚠️ 예전 문장은 "여기서 찍으면 잘 된다" 였다. **실측 후 바꿨다** — 추천 상위는
> 희소성 가중치 때문에 거의 다 경쟁 0편이라, 정의상 "여기서 잘 됐다"는 증거가
> 없는 곳이다. 아래 「핵심 가설 검증 결과」를 읽지 않고 이 문장을 되돌리지 말 것.

화면이 두 갈래다. **둘을 섞지 말 것.**

| | 하는 일 | 근거 |
|---|---|---|
| `/` 홈 | 채널을 넣으면 **골라 준다** (예측) | 약하다. 채널이 성과의 74%.<br>12개 주력 소재는 실데이터, 나머지는 시연 |
| `/start` `/subject` | 소재를 고르면 **다 펼쳐 놓는다** (목록) | 필요 없다. 있으면 있는 것 |

전체 설계는 `SPEC.md` 참조. **작업 시작 전 반드시 읽을 것.**

---

## 이 프로젝트에서 절대 하면 안 되는 것

아래는 "단순화하면 편해 보이지만 서비스 목적이 깨지는" 것들이다.
구현 중 이 방향으로 가고 싶어지면 **멈추고 사용자에게 물어볼 것.**

### 1. 언어별 점수판을 하나로 합치지 말 것
한국어 영상과 영어 영상은 먹히는 소재가 정반대다.
(재래시장: 영어권 4.1배 / 한국어권 0.6배)
합치면 모든 태그가 평균으로 수렴해 추천이 무의미해진다.

### 2. 태그 폴백 규칙을 생략하지 말 것
표본 5편 미만 태그는 상위 태그 점수를 쓴다.
생략하면 우연히 대박난 영상 2편이 태그 순위를 지배한다.

**단 5편은 순위를 쓸 자격이지 배수를 그릴 자격이 아니다.**
배수(`3.2×`)는 100편 이상일 때만 (`MIN_SAMPLE_FOR_MULTIPLIER`). 실측에서
소재당 104편으로도 1.5배 차이의 검정력이 27% 다 — 그 밑에서 낸 숫자는 장식이다.

### 3. 희소성 가중치를 빼지 말 것
`1 / log(1 + 영상수)` 가 없으면 서울/부산만 추천된다.
공모전 주제(인구감소지역)와 정반대 결과가 나온다.

**단 이건 정책이지 예측이 아니다.** 희소 지역이 더 잘 된다는 증거는 없다
(곡성 0.79배 · 구독자 3구간 전부 낮음). 화면은 `경쟁 영상 0편` 이라는 **사실만**
말하고 "기회"·"선점 이익"으로 읽히게 만들지 말 것.

### 4. 근거 표시를 요약하지 말 것
S4 상세 화면의 6단 구조는 **의심을 순서대로 걷어내는 장치**다.
특히 ③ "별로라서가 아니다" 를 빼면 크리에이터가 움직이지 않는다.

### 5. 화면에서 YouTube API를 직접 호출하지 말 것
쿼터 소진으로 시연 중 서비스가 멈춘다. 모든 조회는 자체 DB에서.

### 6. 예상 조회수를 단일 숫자로 쓰지 말 것
반드시 범위 + 추정 고지. 과장하면 신뢰를 잃는다.

### 7. "데이터 없음" / "미개척" 이라고 쓰지 말 것
영상이 없는 곳은 나쁜 곳이 아니라 **가장 좋은 후보**다.
다만 크리에이터에게 "아무도 안 갔다"는 **잘 될 증거가 없다는 신호**로 읽힌다.
실제 크리에이터는 잘 된 영상을 참고해 똑같이 따라 만든다.

→ 같은 사실을 **경쟁 영상 수**로 말한다. `경쟁 영상 0편` + 비교군(정선 14편) 병기.

### 8. 유명한 장소를 화면에서 빼지 말 것
정선 5일장은 추천 대상이 아니라 **증거**다.
크리에이터는 아는 곳으로 서비스를 테스트한다. 정선이 안 나오면 데이터를 의심하고 이탈한다.
그리고 정선이 있어야 곡성이 좋아 보인다.
**단 같은 목록에 섞지 말 것.** 나란히 놓으면 100% 정선을 고른다.

---

## 작업 순서

`SPEC.md` 7장의 8단계를 순서대로 진행한다.
**단계를 건너뛰거나 병합하지 말 것.** 문제가 생겼을 때 원인 추적이 불가능해진다.

각 단계 완료 시 사용자에게 결과를 보여주고 승인을 받는다.
특히 다음 두 지점은 반드시 육안 검증:
- 1단계 후: TourAPI 소개글 채워진 비율
- 6단계 후: 숨 스코어 상위 30개

---

## 🚨 핵심 가설 검증 결과 (2026-08-12) — 읽지 않고 점수를 손대지 말 것

`npm run eval:hypothesis`. 서비스 전체가 서 있던 두 가정을 처음으로 측정했다.

### 대표 문장이 바뀌었다

> ~~여기서 찍으면 잘 된다~~ → **이 소재가 반응이 좋고, 여기서도 찍을 수 있다**

희소성 가중치 때문에 추천 상위는 거의 전부 `경쟁 0편`이다. **정의상 여기서 잘 됐다는
증거가 없는 곳**이다. 증거가 없어서 올라온 곳에 "증거가 있다"고 말하는 구조였다.
두 번째 문장만 방어 가능하다.

### 가설 A · 소재가 성과를 예측하는가 — ✅ **그렇다**

처음 31편 표본으로는 안 보였다. 소재당 300편(`yt:search --plan subject --pages 6`,
7,200 units)을 모으고 나서 갈렸다.

```
묶음 16개 · 영상 1,669편
최고 섬 여행 1.692  ↔  최저 사찰 여행 0.493      격차 3.43배 (기하평균)

전체 검정            p = 0.0093
구독자 구성 보정 후   p = 0.0007   ← 채널 크기 구성 때문이 아니다
```

⚠️ **쌍끼리 비교부터 하면 안 된다.** 120쌍을 검정하면 효과가 없어도 6쌍은 유의하게
나온다. 처음 돌렸을 때 9쌍이 유의했는데 우연 기대치가 6쌍이었다 —
그걸 근거로 결론을 냈으면 거짓말이 될 뻔했다.
BH 보정하면 **통과하는 쌍이 0개다.**

→ **소재 전체가 성과를 가르는 건 맞지만, 개별 소재끼리 "A가 B보다 낫다"고는 못 한다.**
   점수는 **순위**로 쓰고, 근소한 차이를 의미 있는 것처럼 그리지 말 것.

**소재별 표는 `npm run eval:hypothesis` 로 언제든 다시 본다.** 지금 눈에 띄는 것:

| 소재 | 1만 미만 | 1만~10만 | 10만~100만 |
|---|---|---|---|
| **폐교** | 1.06 | **2.59** | **2.41** |
| 섬 여행 | 2.99 | 0.50 | 0.87 |
| 오일장 | 1.64 | 0.42 | 0.81 |
| 사찰 | 0.79 | 0.03 | — |

> **폐교만 채널이 커져도 안 죽는다.** 나머지는 전부 1만 구독을 넘기면 급락한다.
> 폐교는 TourAPI 에 없어서 우리가 직접 승격시킨 소재다 (334곳 · 감소지역 194).
> **이 서비스가 가진 것 중 가장 값어치 있는 발견이다.**

### 계산 방법이 두 가지 바뀌었다

**① 중앙값 → 기하평균** (`score.ts` `geoMean`)

```
1.5배 차이를 80%로 잡는 데 필요한 편수
  중앙값    약 800편
  기하평균  약 300편    ← 2.5배 효율적
```

vsr 은 꼬리가 극단적으로 길다. 중앙값은 이상치에 강한 대신 정보를 너무 버린다.
배수를 다루는 값이라 로그를 씌우면 곱셈이 덧셈이 되고 분포도 정규에 가까워진다.

**② 배수를 그릴 자격을 따로 뒀다** (`MIN_SAMPLE_FOR_MULTIPLIER = 100`)

SPEC 의 폴백 기준 5편은 **순위에나 쓸 수 있는 숫자다.** 5편으로 낸 배수는
신뢰구간이 사실상 전 구간이라 장식이다. 표본 100편 미만이면 순위만 쓰고
`3.2×` 를 화면에 안 그린다. 표본 없이 배수를 그리면 다음 사람이 그 숫자를
근거로 4시간을 운전한다.

### 어필 방식이 바뀌었다 — **예언에서 목록으로** (사용자 지시)

> (X) 여기 가면 잘 된다
> (O) **이 소재를 찍을 수 있는 곳은 이만큼 있다. 영상이 적던 곳도 선택지가 된다**

예측은 근거가 약하다 — 소재 효과는 실재해도 채널이 74% 를 설명하고, 개별 소재끼리는
BH 보정을 통과하는 쌍이 0개다. 반면 **목록은 근거가 필요 없다. 있으면 있는 것이다.**

`npm run report:inventory` — 목록이 얼마나 채워졌는지 본다.

```
좌표 90%+ · 인구감소지역 30곳+ 인 소재   45종
장소 합계 20,934곳 (감소지역 6,928곳)
```

카드 판정 세 단계: `목록만`(영상 표본 없음) / `순위만`(<100편) / `배수까지`(100편+).

⚠️ **사진이 점수보다 중요해졌다.** 목록으로 어필하면 화면이 썸네일로 채워진다.
   사진 30% 미만: 폐교 0% · 간이역 16% · 게스트하우스 25%.

### 🚨 폐교 좌표를 채웠다 — 카카오 키 없이 (2026-08-12)

`npm run fill:schoolcoords`. **가장 값어치 있는 소재가 지도에 점 하나 못 찍고 있었다.**

표준데이터에 위경도 열이 없는데, 주소에는 읍면리가 있다. 그리고 우리에겐
같은 읍면의 TourAPI 장소 좌표 48,903곳이 이미 있다. 그 평균을 쓴다.

```
읍면 중심   290곳   쓸 만하다 (읍면 하나 수 km)
시군구 중심  44곳   ⚠️ 거칠다
실패          0곳
```

⚠️ **읍면 중심은 학교 건물 위치가 아니다.** `place.coord_source` 에 출처를 남겼다
   (`원본` / `읍면추정` / `시군구추정`). 지역코드와 같은 원칙 — 추정을 원본인 척 섞지 않는다.
   카카오·V-World 키가 생기면 덮어쓴다.

### 🚨 소개글 수집이 몇 주째 쇼핑만 갈고 있었다 (2026-08-12)

받아둔 2,995건이 **전부 콘텐츠타입 38(쇼핑)** 이었다. 사찰·계곡·해수욕장은 **0건**.

```
타입 38 전체 12,248 · 받은 것 2,996 · 남은 것 9,252
→ 관광지(12)로 넘어가려면 9일을 더 쇼핑에만 써야 했다
```

게다가 쇼핑 대부분은 대형마트·백화점·면세점이라 `SUBJECT_SKIP` 으로 이미 제외한 곳이다.

**오류가 안 뜬다.** 리포트에는 "소개글 2,995건"으로 찍혀서 잘 되는 것처럼 보인다.
소재별로 갈라 보고 나서야 드러났다.

→ 우선순위를 **콘텐츠 타입이 아니라 소재로** 바꿨다 (`collect-overview.ts`).
  ① 12개 주력 소재 × 인구감소지역 2,410곳 → ② 주력 소재 나머지 2,097곳 → ③ 태그 있음 → ④ 나머지
  **5일이면 12개 소재가 다 찬다.**

> 교훈: 총량만 보면 안 된다. **쓸 곳 단위로 갈라서 봐야 빈 곳이 보인다.**

### 소재 × 지역 격자 — **시군구 필터는 절반이 빈다** (2026-08-12)

`npm run report:grid`. 목록 화면을 만들기 전에 "빈 목록이 얼마나 나오는지" 부터 쟀다.
전국 합계만 보면 넉넉해 보이는데, 지역으로 좁히는 순간 무너진다.

```
소재 × 시군구   1곳+ 52%   3곳+ 29%   5곳+ 17%   ← 절반이 0곳
소재 × 시도                          5곳+ 67%
지역만 (소재 무관, 12종 합계)          5곳+ 98%
```

| 소재 | 장소 | 시군구 1곳+ | 5곳+ |
|---|---|---|---|
| 유적지/사적지 | 1,277 | 86% | 100곳 |
| 사찰 | 725 | 87% | 60곳 |
| 5일장 | 458 | 72% | 35곳 |
| 폐교 | 334 | 50% | 18곳 |
| 자연휴양림 | 113 | 37% | **0곳** |

→ **소재 + 시군구 조합을 화면에 주면 안 된다.** 눌렀는데 빈 화면이 나오면
  크리에이터는 데이터가 없다고 판단하고 이탈한다 — 실제로는 옆 시군구에 있는데도.

→ 지역 좁히기는 **시도** 단위. 시군구는 **소재를 풀 때만** (그때는 98% 채워진다).

**빈 칸에 두 종류가 있다. 섞지 말 것.**
- **사실** — 내륙에 해수욕장(22%)·섬(17%)·항구(25%)가 없는 건 지형이다. 고칠 게 없다
- **결함** — 폐교 50% 는 전국에 있는데 미활용분만 골라서 그렇다. 수집으로 메운다

#### 그래서 지역 칩을 하드코딩하지 않는다

`npm run export:availability` → `src/data/real/availability.json` (소재 82종)

소재마다 **장소가 실제로 있는 시도·시군구만** 담는다. 시군구는 3곳 이상만.
화면은 이 목록으로만 칩을 그린다 — **0곳인 지역은 선택지에 아예 없다.**
그러면 빈 화면이 구조적으로 안 나온다.

시군구 칩이 하나도 없는 소재 7종(문화원·호수·수련시설 등)은 전국·시도 목록으로만 쓴다.

### 분산 분해 · 소재는 **부차적이다**

```
채널이 설명하는 몫      74.2%
채널 안에서 갈리는 몫    25.8%   ← 소재가 다툴 수 있는 최대치
```

소재 효과는 실재하지만(p=0.0007) **크리에이터 본인의 실력이 3배 더 크다.**
화면이 "여기 가면 잘 된다"는 인상을 주면 안 되는 또 하나의 이유다.

### 가설 B · 비어 있는 곳이 기회인가 — **뒷받침되지 않는다**

```
곡성(인구감소·희소)  1.332      국내여행 일반  1.681      0.79배  p=0.486
```

p 값만 보면 "차이 없음"이지만 **구독자 구간을 갈라 보면 3구간 전부 곡성이 낮다**
(0.83 / 0.52 / 0.79배). 우연이라면 1/8. 유의하진 않아도 **방향이 일관되게 불리하다.**

→ 희소성 가중치를 **경험적 예측이 아니라 정책 가중치로 재정의할 것.**
   "비어 있어서 잘 된다"는 근거가 없다. "여긴 경쟁이 없다"는 사실만 말하고
   판단은 크리에이터에게 넘긴다. (공모전 주제상 인구감소지역을 올리는 것 자체는 유지)

### 표본 설계를 바꿔야 한다

검색은 **채널당 1~2편**만 물어온다 (161채널 중 128개가 1편). 그래서 "같은 채널 안에서
소재를 바꿔 본다"는 가장 깨끗한 설계가 안 돌아간다.
→ 검색으로 채널을 **발견**하고, 그 채널을 `yt:channel` 로 훑어 **채널당 여러 편**을 모을 것.

---

## 현재 상태

- [x] 0. 가짜 데이터로 화면 뼈대 — S1~S5 완료. `/check` 에서 예외 케이스 10종 자동 점검
- [~] 1. TourAPI 전수 수집 — 목록 완료(64,037). 소개글 국문 2,995 / 영문 2,972
      **수집 우선순위를 소재로 바꿨다 — 5일이면 12개 주력 소재가 찬다** (아래 참조)
      매일 09:30 자동 수집 중 (`매일수집.bat`)
- [x] 2. 보강 데이터 적재 + 중복 정리 — 파일 3종 2,802건 · `place_link` 907건
- [x] 3. 태그 체계 확정 — 213개. 규칙만으로 23,803건(47.6%) 태깅 완료
- [ ] 4. LLM 전량 태깅 — **프롬프트 확정·정확도 96.7% 검증 완료.** 소개글 대기 중
- [~] 5. YouTube 수집 — **영상 42,018편 · 댓글 21,867 · 채널 2,543.**
      12개 주력 소재 × 300편(라벨 있음) + 채널 훑기 38,671편(라벨 없음)
      영어 소재 코퍼스가 비어 있다 → 영어 채널은 점수를 못 낸다
- [x] 6. 점수 계산 — **가설 검증 완료(p=0.0007)** · 기하평균 · 신뢰구간 기준 · `export:tagscores`
- [~] 7. 실데이터 연결 — 아래 표 참조. **남은 건 S4 6단 근거뿐이다**
- [ ] 8. 크리에이터 테스트 — 아직 사람 반응 0건

### 화면별 실데이터 현황 (2026-08-13)

| 화면 | 상태 |
|---|---|
| `/start` `/subject` 목록 | ✅ 전부 실데이터 |
| 홈 · 채널 지표 (롱폼/쇼츠) | ✅ 실측 |
| 홈 · 채널 소재 태그 | ✅ 실측 (`tag:channel` LLM 분류) |
| 홈 · 소재 현황 (성적·잘 된 영상·촬영 완료) | ✅ 12개 소재 실데이터 (`realSubjectEvidence`) |
| 홈 · 추천 카드 | ✅ 12개 소재 실데이터 · 나머지는 시연 |
| `/place/[id]` S4 6단 근거 | ✅ 12개 소재 실데이터 (`realdetail.ts`) |
| `/admin` 기관용 | ❌ 시연 |

### 실데이터 경로는 `repo.ts` **옆에** 있다 (2026-08-13)

`repo.ts` 의 추천·상세 경로는 시연 데이터(`FAKE_*`)와 촘촘히 엮여 있다. 거기를 뜯으면
`/check` 까지 같이 흔들린다. 그래서 실데이터 경로를 **따로 놓고 화면이 그쪽을 먼저 보게** 했다.

```
realcards.ts    홈 추천 카드 + 소재 현황 블록
realdetail.ts   /place/[id] 6단 근거
                → 카탈로그(places.json)에 있는 장소면 실데이터, 없으면 시연으로 폴백
```

다리는 **태그 이름**이다. 홈이 아직 시연 태그 체계(`t_oil_market`)로 돌기 때문에
`subjectForTagName()` 으로 실데이터 소재를 찾는다. 짝이 없으면 시연 카드가 나간다.

⚠️ **실재하는 장소에 지어낸 영상을 붙이면 안 된다.** 가짜 장소에 붙이는 것보다 나쁘다 —
   크리에이터가 그걸 믿고 4시간을 운전한다. 그래서 6단 중 재료가 없는 칸
   (컷 순서·제목 예시·숙소)은 **문장으로 메우지 않고 안 그린다.**

⚠️ **"시연용" 배너를 실데이터 화면에 띄우지 말 것.** `/place/[id]` 는 같은 경로가 장소에
   따라 갈리므로 경로로 못 가른다. 실데이터 화면이 `#real-data-page` 표식을 심고
   `globals.css` 의 `:has()` 규칙이 배너를 지운다 — **CSS 라서 하이드레이션과 무관하다.**
   진짜 데이터를 가짜라고 말하는 건 그 반대만큼 나쁘다.

## ▶ 다음 세션에서 이것부터

**승인은 이제 병목이 아니다.** 기본 쿼터 10,000/일 안에서 여기까지 왔다.
아래는 전부 **오늘 쓸 수 있는 것만으로** 돌아간다.

### 1) 영어 소재 코퍼스 — 7,200 units

지금 영어 채널을 넣으면 `순위만 · 수집 1편` 이 뜬다. 영어 소재 표본이 사실상 없다.
언어별 점수판은 절대 합치지 않으므로(1항) 영어는 영어로 모아야 한다.

```
yt:search --plan subject --pages 6 --lang en    (검색어를 영어로 다시 짜야 한다)
```

⚠️ `SUBJECT_PLAN` 이 국문 검색어뿐이다. 영어판을 먼저 만들 것 —
   `Korea five day market vlog` 처럼 **여행 맥락을 박아야** 먹방·리뷰가 안 온다.

### 2) LLM 소재 분류 확대 — 쿼터 0

어제 사고로 얻은 롱폼 15,412편(설명 100자+)이 **라벨 없이** 쌓여 있다.
`tag:channel` 은 이미 돌아가고 어휘 이탈 0건이다. 채널 100여 개로 넓히면
태그 점수 표본이 수십 배가 된다 → `export:tagscores` 재실행 → 배수를 그릴 수 있는
칸이 17/79 에서 크게 늘어난다.

```
tag:channel --channel <id> --max 60     한국 지명이 잡히는 채널 114개가 후보
export:tagscores → export:places → export:channels
```

### 3) 소개글 — 자동으로 쌓인다

우선순위를 소재로 바꿔서 **5일이면 12개 주력 소재가 다 찬다.** 손댈 것 없다.
차면 `eval:tagging -- --n 200` 재검증 후 4단계 전량 태깅.

### 4) 남은 시연 데이터 걷어내기

`/place/[id]` 의 S4 6단 근거와 `/admin` 이 아직 시연이다.
6단 근거는 재료가 거의 모였다 (경쟁 영상 수·소재 성적·장날·일출·근처).

---

**손대지 말 것**: 카카오 키는 이제 급하지 않다. 폐교 334곳 좌표는
읍면 중심으로 채웠다 (`fill:schoolcoords`). 키가 생기면 정밀도만 올라간다.

### 🚨 쿼터를 쓰는 명령은 반드시 대상을 세고 시작할 것

`yt:channel --all` 이 2,543개를 훑어 하루치를 한 번에 날린 적이 있다(2026-08-13).
`--limit` 없이는 안 돌게 막아뒀지만, **다른 명령도 같은 위험이 있다.**
쿼터는 하루 한 번뿐이라 "일단 돌려보고 판단" 이 안 되는 자원이다.
`--dry` 가 있는 명령은 반드시 먼저 돌린다.

### 신청 현황 (2026-08-12 기준)

문안은 `docs/신청서-문안.md`, 무엇을 어디서 하는지는 `docs/신청-체크리스트.md`.

| 대상 | 상태 |
|---|---|
| YouTube Data API 300,000/일 | ⏳ **2026-08-13 반려 → 같은 날 재제출 완료** · 회신 대기 (아래 참조) |
| 국문·영문 관광정보 (운영계정) | ⏳ 2026-08-11 신청 |
| 기상청 단기예보 · ASOS · 해수욕장 날씨 | ✅ 승인 (개발계정으로 충분) |
| 한국천문연구원 출몰시각 | ✅ 승인 · 실호출 확인 |
| 카카오 REST·JS 키 | ❌ 아직 (심사 없음, 도메인 제한 등록할 것) |

시군구 단위 서비스(연관관광지·기초지자체·수요·다양성)는 다 합쳐야 6,000회라
**상향 신청이 필요 없다.**

#### 🚨 YouTube 1차 반려 — 자료가 아니라 **형식**이 문제였다 (2026-08-13)

> We reviewed your request and did not find sufficient information to conduct an API
> compliance review... Kindly provide a detailed step-by-step visual reference or a
> screen cast of the use case, along with the end results, and the complete
> functionality of the API client.

1차에 낸 건 **정적 스크린샷 7장 + 다이어그램 2장**이었다. 항목은 다 채웠는데
**"이걸 쓰면 무슨 일이 벌어지는가"를 순서대로 보여주는 자료가 없었다.**
게다가 화면이 전부 한국어라 심사자가 뭘 보고 있는지 알 방법이 없다.

→ 재제출 자료는 `docs/youtube-audit/` 에 있다.

```
SOOM-API-Client-Walkthrough.pdf   12쪽 · 영문 · 한 세션을 처음부터 끝까지
walkthrough.html                  PDF 원본. 고치면 아래 명령으로 다시 굽는다
reply-email.md                    답장 문안 + 보내기 전 체크리스트
01~09-*.png                       화면 캡처 (주소 표시줄 포함)
```

```
chrome --headless=new --no-pdf-header-footer --virtual-time-budget=10000 \
       --print-to-pdf=SOOM-API-Client-Walkthrough.pdf walkthrough.html
```

**화면마다 어느 API 호출이 어느 픽셀을 만들었는지** 영문으로 적었다. 이게 요구의 핵심이다.

⚠️ 증빙 캡처는 `scripts/capture-chrome.ps1` 로 뜬다. **확장 프로그램 캡처는 안 된다** —
   페이지 영역만 나와서 주소 표시줄이 빠진다. 이 스크립트는 크롬을 앞으로 끌어내고
   인포바("디버깅을 시작함")를 닫은 뒤 화면 전체를 뜬다. 인포바는 **새로고침마다
   다시 뜨므로 캡처 직전에 매번** 닫아야 한다.

⚠️ **재제출 전에 시연 데이터를 걷어내야 했다.** 유튜브 영상이 나오는 화면이 시연이면
   "전체 기능을 보여달라"는 요구를 만족할 수 없다. 홈 소재 현황과 S4 6단이 그래서
   실데이터로 바뀌었다 (위 「실데이터 경로」 참조).

#### YouTube 심사에 낸 것 (재신청·재감사 대비)

증빙은 바탕화면에 그대로 있다. 재감사 요청이 오면 같은 것을 다시 낸다.

```
privacy-policy-page.png        개인정보처리방침 (5·7절이 한 화면에)
homepage-youtube-branding.png  홈 푸터 — YouTube 브랜딩 + 방침 링크
terms-of-service.pdf           이용약관
youtube-player-embed.png       /data-sources 의 실제 임베드
architecture-diagram.pdf       3단 구조 (배치 → 자체 DB → 화면)
user-flow-diagram.pdf          5단계 흐름 · YouTube 데이터 진입 지점
analytics-dashboard.png        추천 화면
```

⚠️ 증빙 스크린샷은 **브라우저 주소 표시줄이 보여야 한다** (품질 표준).
   확장 프로그램 캡처는 페이지 영역만 찍혀서 안 된다 — OS 레벨로 창 전체를 떠야 한다.

⚠️ **폼에 새로고침 금지.** 임시저장이 없어서 전부 날아가고, 첨부 파일은 특히 복구 불가다.
   제출이 막히면 섹션이 접혀 있거나(2~7) `추가할 프로젝트 번호`가 비어 있는 것부터 본다.
   프로젝트 번호는 `507307814114` (GCP 프로젝트 `travel-505308`).

### 추천 카드 (2026-08-05 재구성 완료)

접힌 카드에 두는 것은 **네 가지뿐**이다. 여기에 항목을 다시 늘리지 말 것 —
5장을 훑는 동안 아무것도 눈에 안 들어오게 된다.

```
[썸네일]  장소명 · 시도 시군구 · 인구감소
          {성과}  경쟁 N편
          {성과 출처}
          [다른 소재 태그]
          구성 보기 ▾
```

**성과 줄이 이 카드의 핵심이다.** 두 가지 중 하나가 들어간다 (`display.performanceLine`).

| 조건 | 표시 | 뜻 |
|---|---|---|
| 이 장소 영상 ≥ 5편 | `여기서 찍은 8편 · 2.9×` | 여기서 실제로 나온 성적 |
| 그 외 (대부분) | `오일장 영상 3.2×` + `정선 5일장 · 화개장터 기준` | 같은 소재가 **다른 지역**에서 낸 성적 |

두 줄을 같은 모양으로 그리지 말 것. 추천 상위는 희소성 가중치 때문에 거의 다
경쟁 0편이라 아래쪽이 기본값인데, 출처를 안 밝히면 "여기서 3.2× 나왔다"는 거짓말이 된다.

**펼침**은 페이지를 떠나지 않는다 (크리에이터가 5곳을 오가며 비교한다).
`잘 된 영상의 구성 → 찍을 수 있는 컷 → 묶어서 찍을 곳 → 운영 정보 → 근거 6단 링크`

- **근거 6단(S4)은 펼침에서 요약하지 않는다.** 링크로만 넘긴다 (위 4항)
- 소재 태그 칩에서 **지금 고른 소재는 뺀다.** 성과 줄과 같은 값이라 카드마다 똑같은 칩이 반복된다

카드에 넣지 않기로 한 것:
- **예상 도달** — 소재 점수 × 구독자라서 5장이 전부 같은 값이 된다. 소재 현황 블록에 한 번만 둔다
- **숨 스코어 숫자·막대** — 순위가 이미 그 값이다. `/check` 에서만 본다

---

> TourAPI 키 발급 대기 중. 데이터 수집(1단계)은 그 다음.

### 0단계 결과물

| 파일 | 역할 |
|---|---|
| `src/lib/types.ts` | SPEC 3장 데이터 모델 + **태그 6축**. snake_case 유지 → DB 컬럼과 1:1 |
| `src/lib/score.ts` | SPEC 4장 계산식. **6단계에서 그대로 재사용한다** |
| `src/lib/display.ts` | SPEC 9장 표시 원칙. 화면은 여기 거쳐서만 라벨을 만든다 |
| `src/lib/geo.ts` | 좌표 계산. 근처 소재 찾기 + 2단계 중복 판정에 쓴다 |
| `src/lib/i18n.ts` | 문구 단일 소스 |
| `src/lib/repo.ts` | **7단계에서 바꾸는 유일한 파일.** 화면은 여기만 부른다 |
| `src/app/globals.css` | 색·글꼴 토큰. **톤을 바꾸려면 여기만 고친다** |
| `src/components/MapHero.tsx` | 도트 지도 + 공항 노선. **홈 히어로 배경에만 쓴다** |
| `src/components/PlaceThumb.tsx` | 카드 썸네일. `image_url` 이 차면 **코드 수정 없이** 실사진으로 바뀐다 |
| `src/components/VideoBreakdownCard.tsx` | 잘 된 영상 한 편의 구성. 챕터 출처를 반드시 함께 적는다 |
| `src/data/fake/*` | 가짜 데이터. 7단계에서 통째로 버린다 |

### 화면 구조 (0단계 완료 시점)

```
/                 홈. 입력 → 분석 → 추천이 한 페이지에서 끝난다 (예측 · 시연 데이터)
/start            소재로 찾기. 12개 소재 인덱스        ← 여기부터 실데이터
/subject/[slug]   소재별 촬영지 목록 + 시도 칩 + 장날
  ?q=채널명        히어로에 채널 분석이 뜨고 아래 추천 플로우
  ?tag=t_xxx      소재 변경 (페이지 이동 없이 재계산)
  ?guest=1&tag=   채널 없이 소재만 고른 경로
/place/[id]       장소 상세. 사진 → 정보 → 6단 근거
/admin            기관용
/check            개발용 검증표 (발표에 안 나감)
/profile /recommend   옛 경로. 홈으로 리다이렉트만 한다
```

### 문구 원칙 (사용자 지시)

- **빈 자리를 문장으로 채우지 말 것.** 데이터가 없으면 섹션 자체를 그리지 않는다
- 설득·설명 문장 금지. 라벨과 숫자만 두고 판단은 크리에이터가 한다
- 형용사 대신 숫자. 배수는 `3.2×` 로 표기
- "추정입니다" 류 고지 최소화 (예상 도달의 SPEC 필수 고지만 남김)

### 배포 (Netlify)

**https://gwanjetap.netlify.app** — 계정 god77712 (떡잎동사무소) · 사이트명 `gwanjetap`

```
netlify deploy --build --prod
```

배포 목적이 **data.go.kr 한도 상향 신청에 넣을 운영 URL 확보**였다.
신청서에 서비스 URL 을 요구하는데 없으면 신청 자체가 안 된다.

- 지금 올라간 건 0단계 결과물(가짜 데이터)이다. **키가 하나도 없어도 빌드되고 돌아간다**
- 사이트명은 Netlify 대시보드에서 언제든 바꿀 수 있다 (URL 도 따라 바뀐다)
- `.env` 는 올리지 않는다. 화면은 키를 쓰지 않는다 (SPEC 6장 절대원칙 1)
- **배포 전 `npm run dev` 를 끌 것.** `.next` 를 서로 덮어써서 빌드가 깨진다

### 명령어 — 순서가 있다

가공 스크립트는 앞 단계 결과를 읽는다. **순서를 지키지 않으면 조용히 빈 결과가 나온다.**

```
수집 (쿼터 소모)
  collect:list          장소 목록          collect:category   분류코드 이름
  collect:overview      소개글 (--en 영문)  collect:extra      사진·수상작·걷기길
  yt:channel @핸들      채널 최근 50편 · 3 units
  yt:search             소재·지역 검색 · 페이지당 100 units  (--dry 로 먼저 확인)
  yt:comments           댓글 · 영상당 1 unit

가공 (순서 고정)
  load:raw  →  dedup  →  build:places  →  build:tags
  파일 적재     중복 연결   장소 마스터      태그 + 규칙 태깅

검증
  report:stage1     수집 현황
  eval:tagging      LLM 태깅 정확도 (정답지 대조)
  eval:videoplace   영상 → 장소 연결이 되는지

내보내기
  export:share      공모전 제출용 CSV
  export:channels   화면용 채널 JSON  ← 이걸 돌려야 화면에 실채널이 뜬다
```

**매일 09:30 자동 실행**: `매일수집.bat` (작업 스케줄러 `SOOM 소개글 매일수집`).
국문 → 영문 → 현황 리포트를 `data/daily.log` 에 append 한다.

⚠️ `.bat` 은 **ASCII 로만 쓴다.** cmd.exe 가 `chcp` 보다 먼저 바이트 단위로 파싱해서,
   UTF-8 한글 주석이 있으면 그 뒤 모든 줄이 깨진다. 처음 버전이 그렇게
   **아무것도 안 하고 종료 코드 0** 을 냈다 — 성공한 것처럼 보이는 실패다.
   `npm` 이 아니라 `node` 를 직접 부르는 것도 같은 이유(스케줄러 PATH 가 다르다).

### 개발 메모

- **`실행.bat` 더블클릭**하면 서버가 켜지고 브라우저가 열린다
- `npm run dev` 와 `npm run build` 를 **동시에 돌리지 말 것.** `.next` 를 서로 덮어써서 깨진다.
  깨지면 서버 끄고 `.next` 지우고 다시 `npm run dev`
- `npm run probe` — TourAPI 9개 서비스 연동 상태 재확인
- 카카오 공유는 `NEXT_PUBLIC_KAKAO_JS_KEY` 를 넣으면 코드 수정 없이 켜진다 (지금은 링크 복사로 동작)
- **계산으로 만든 SVG 좌표는 반드시 `toFixed()` 로 자릿수를 고정할 것.**
  실수를 그대로 넣으면 서버와 브라우저가 마지막 자리를 다르게 찍어 하이드레이션이 깨진다.
  깨지면 화면은 멀쩡해 보이는데 `onClick` 이 안 붙어서 **클릭만 조용히 안 먹는다** (PlaceThumb 에서 겪음)

---

## 기술 메모

### 지역코드는 `scripts/lib/region.ts` 하나로만 붙인다 (2026-08-11)

TourAPI 는 지역코드가 절반 넘게 비어 있고(48,929 중 26,635), 보강 파일 3종은
아예 코드가 없이 주소만 온다. 양쪽을 **같은 기준**으로 시군구에 붙여야
"인구감소지역에 이 소재가 몇 개" 를 셀 수 있다. 그래서 판정을 한 곳에 모았다.

4단으로 내려가며 붙인다. 어떤 단계에서 붙었는지 `RegionSource` 로 끝까지 들고 다닌다 —
**추정값을 원본인 척 섞으면 안 된다.**

```
① 원본 코드        22,294
② 주소 문자열      26,204
③ 최근접 좌표         429   ← 같은 시도 안에서만 찾는다
④ 미상                  2
```

실측으로 걸러낸 함정 넷. 전부 하드코딩이 아니라 규칙으로 처리했다:

| 증상 | 원인 | 처리 |
|---|---|---|
| `전북특별차치도` 61건 | 전통시장 표준데이터 원본 오타 | 별칭에 추가 |
| `세종특별자치시 전의면` | 세종은 시군구 계층이 없다 | 시도=시군구인 곳 따로 |
| `경상북도 군위군` | 2023년 대구로 편입, 원본은 옛 시도 | 시군구명이 전국 유일하면 시도 무시 |
| `전남광주통합특별시` 약 2,000건 | 시도 통합, 코드표는 옛 체계 | 옛 시도 후보를 순서대로 대본다 |

`인천 제물포구·영종구·서해구·검단구` 429건은 코드표에 아예 없는 신설·개명
자치구라 좌표로 붙였다. **같은 시도로 후보를 좁히지 않으면** 검단구가 바로 옆
김포시로 넘어간다 (실제로 4건 그랬다).

> 서해구는 강화·옹진이 아니라 **서구가 이름만 바뀐 것**이다 (주소가 가좌동·청라동).
> 이름만 보고 짐작하지 말 것.

### 보강 파일 3종 — 받아보니 이랬다 (2026-08-11)

`npm run load:raw`. 파일명에 날짜가 붙어 매번 바뀌므로 `data/raw/` 에서 키워드로 찾는다.

| 파일 | 건수 | 건진 것 |
|---|---|---|
| 전국전통시장표준데이터 | 1,393 | **`시장개설주기`가 `2일+7일` 형태로 들어 있다** |
| 전국폐교재산기본정보 | 1,194 | 미활용 334 · 대부 532 · 자체활용 328 |
| 국가철도공단 철도역 | 215 | `열차정차횟수` → 정차 10회 이하 51곳 |

**장날을 LLM 으로 뽑을 필요가 없어졌다.** 소개글에서 추출하려던 계획(4단계)은
정기장 411곳에 대해서는 이 파일이 더 정확하다. LLM 은 여기 없는 곳만 맡는다.

폐교는 **'미활용' 334곳만 촬영 후보다.** 대부·자체활용은 이미 남이 쓰고 있다.
그중 194곳이 인구감소지역에 있다.

⚠️ 철도역 CSV 는 **EUC-KR** 이다. UTF-8 로 읽으면 전부 깨진다.
   표준데이터 `.xls` 는 구형 BIFF 라 `xlsx` 패키지가 필요하고, **첫 줄이 제목이라
   `range:1` 로 건너뛰지 않으면** 열 이름이 전부 `__EMPTY_n` 이 된다.

### 중복 정리는 **지우지 않고 잇는다** (2026-08-12)

`npm run dedup` → `place_link` 테이블. 규칙은 `scripts/lib/match.ts`.

정선 5일장은 TourAPI 와 전통시장 표준데이터 양쪽에 있다. **어느 쪽도 버릴 수 없다** —
TourAPI 에는 소개글(태그 원료)이, 표준데이터에는 장날·점포수가 있다. 그래서 짝만 남긴다.

```
전통시장  1,393 중 665 연결   폐교 1,194 중 6   철도역 215 중 51   내부중복 185쌍
장날과 소개글을 둘 다 가진 곳 197 → 4단계 태깅 최우선 대상
```

**연결이 안 된 쪽이 오히려 값어치다.** 미활용 폐교 334곳은 TourAPI 에 하나도 없다.
경쟁 영상이 없는 게 아니라 **아무도 데이터로 만들지 않은 소재**라는 뜻이다.

실측으로 잡은 오탐 셋. 전부 "이름만 보면 맞아 보이는데 틀린" 것들이다:

| 오탐 | 왜 생겼나 | 처리 |
|---|---|---|
| `정선아리랑시장` ↔ `정선아리랑제` | **축제를 장소로 봤다.** 359m·유사도 0.89 로 진짜 짝을 이겼다 | 축제(15) 제외 + 소재별 선호 타입 |
| `남대문시장` ↔ `숭례문(남대문) 수입상가` | 괄호 별칭 한 조각만 맞아 1.00 | 조각은 이름에서 차지하는 비중만큼만 인정 |
| `롯데백화점 건대점` ↔ `반스 롯데백화점 건대점` | 입점 매장 | 한쪽이 다른 쪽 **뒤에 통째로** 붙으면 제외 |

> 이름 유사도만으로는 반드시 틀린다. **거리·콘텐츠 타입과 항상 함께 봐야 한다.**
> `중앙시장` 은 전국에 수십 개고, `정선아리랑제` 는 이름이 더 비슷하다.

내부 중복은 **정밀도가 재현율보다 중요하다.** 다른 두 곳을 하나로 묶으면 추천에서
한 곳이 통째로 사라지지만, 못 묶으면 비슷한 게 두 번 보일 뿐이다. 그래서 100m·0.9 로 엄하게 잡았다.

### 태그 체계는 **새로 만들지 않았다** (2026-08-12)

`npm run collect:category` → `npm run build:tags`

설계하려고 데이터부터 봤더니 관광공사 분류가 이미 **대분류 24 · 세부 153** 이었다.
SPEC 이 잡았던 "대분류 20 + 세부 150" 과 사실상 같고, 필요하다고 적어둔
`5일장`·`등대`·`상설시장` 이 전부 소분류에 있다.

> 새로 만들면 두 분류 사이 대응표를 평생 관리해야 한다.
> 그 표가 틀어지는 순간 **점수가 조용히 어긋난다.** 그대로 쓰고 없는 것만 얹었다.

```
subject   180 = 관광공사 대분류 24 + 세부 153 + 유휴공간 3(폐교·간이역)
나머지 축  33 = time · mood · format · persona · audience
합계      213
```

**계층을 cat2/cat3 에서 잘랐다.** cat1 은 7종("자연","인문")이라 폴백 단위로 너무 거칠다 —
표본 부족일 때 "자연 전체 평균"을 빌려오면 등대가 국립공원 점수를 쓰게 된다.

나머지 축 어휘도 지어내지 않고 **관광사진 6,118장의 `search_keyword` 빈도**에서 골랐다
(드론촬영 552 · 단풍 187 · 설경 155 · 파노라마 141 · 야경 122 · 일몰 88).

축마다 **근거가 다르다.** 이걸 지키지 않으면 변별력이 0 이 된다:

| 축 | 근거 | 이유 |
|---|---|---|
| subject | rule → overview | 분류코드로 절반, 나머지는 소개글 |
| time | overview | "일출 명소" 처럼 사실로 적혀 있다 |
| **mood** | **comment** | 소개글에서 뽑으면 **전부 '정겨움'이 된다.** 홍보문이라서 |
| format · persona | video | 어떻게 찍혔는지는 영상만 안다 |

### cat3 가 45.8% 뿐인 게 오히려 이득이다

지역코드가 빈 행과 같은 행들이다. 규칙으로는 22,425건밖에 못 붙인다.

**그 22,425건이 정답지다.** LLM 을 나머지 26,414건에 돌리기 전에 여기서 정확도를 재고
프롬프트를 고친다. 검증 없이 전량 돌리면 틀린 태깅 4만 건을 나중에 발견한다.
→ 4단계는 **정답지 평가부터** 시작할 것.

### 장소 마스터 — 보강 데이터를 승격한다 (2026-08-12)

`npm run build:places` → `place` 표 (SPEC 3장). **`dedup` 다음, `build:tags` 앞에 돌린다.**

3단계 태깅을 처음 돌렸더니 폐교·간이역이 9건밖에 안 붙었다. 미활용 폐교 334곳이
**TourAPI 에 아예 없어서** 태그를 붙일 대상 자체가 없었다. 하필 이게 주력 소재다 —
아무도 데이터로 만들지 않아서 값어치가 있는 것인데, 목록에 없으면 추천이 안 된다.

```
TourAPI     48,929 (high)
전통시장 승격    728 (medium · 정기장 184)
폐교 승격        334 (low · 미활용만)
간이역 승격       42 (low · 정차 10회 이하)
──────────────────────
합계        50,033   인구감소지역 12,904 (25.8%)
```

이어둔 짝(`place_link`)은 승격하지 않는다. 같은 곳이 두 번 들어간다.

**`data_reliability` 를 반드시 남긴다.** 폐교·간이역은 현장 상태가 자주 바뀌어
화면에 "공공데이터 기준, 현장 확인 권장"을 띄워야 하는데(SPEC 9장),
출처를 지우고 한 표에 섞으면 그 경고를 띄울 근거가 사라진다.

⚠️ 폐교 334곳은 **좌표가 없다.** 표준데이터에 위경도 열이 자체가 없다. 주소로 채워야 한다.

### 🚨 영문 수집이 국문 수집을 영구히 막고 있었다 (2026-08-12)

`tour_overview.status` 한 칸을 국문·영문이 같이 썼다. 영문이 먼저 받은 행은
`status='ok'` 가 되는데, **국문 큐는 "행이 없거나 fail 인 것"만 잡는다.**
→ 영문이 먼저 닿은 content_id 는 국문 소개글을 **영원히 못 받는다.**

발견 시점에 **1,984건**이 잠겨 있었고, 두 수집기를 같이 돌릴 때마다 하루 1,000건씩 늘었다.
국문 소개글은 4단계 태깅의 유일한 원료라 그대로 뒀으면 태깅 대상이 계속 줄어든다.
**오류가 안 뜬다.** 리포트에는 "있음 3,977" 로 찍혀서 잘 되는 것처럼 보인다.

→ `status_en` 열을 따로 뒀다. `openDb()` 의 `patchColumns` 가 기존 DB 를 옮기면서
   잠긴 행을 자동으로 푼다 (SQLite 는 NOT NULL 을 못 떼서 표를 새로 만든다).

> 교훈: **한 열에 두 주체가 쓰면 안 된다.** 수집기가 둘이면 상태 칸도 둘이어야 한다.

### 4단계 태깅 — 정답지로 먼저 재고 나서 돌린다 (2026-08-12)

`npm run eval:tagging -- --n 60`. 프롬프트는 `scripts/lib/tagger.ts`.

소개글과 cat3 를 **둘 다** 가진 627건이 정답지다. 전량에 돌리기 전에 여기서 잰다.

```
소분류까지 정확  76.7%
대분류는 맞음    20.0%   ← 6단계 폴백이 이 단위로 돈다
틀림              3.3%
목록에 없는 답    0%     ← 지어내지 않았다
─────────────────────
쓸 만함          96.7%
```

**표본을 희귀 분류 우선으로 뽑았다.** 그냥 뽑으면 음식점이 4분의 1이라 '한식'만 맞춰도
점수가 부풀려진다. 실제 태깅 대상의 난이도에 맞춘 수치다.

> ⚠️ **틀린 2건 중 하나는 정답지가 틀렸다.**
> `으능정이문화의거리` 의 cat3 가 `스키(보드) 렌탈샵` 으로 붙어 있다. LLM 은 `이색거리` 로 봤고
> 그게 맞다. → **96.7% 는 하한이다.** 정답지에도 오류가 있다.

⚠️ **시간대·계절은 1.7% 밖에 안 붙었다.** 소개글에 "일출 명소" 처럼 사실로 적힌 경우가
   드물다. time 축을 소개글에만 의존하면 거의 빈다 — 5단계 영상·사진 키워드로 보강해야 한다.

**비용: 전체 48,929건에 $182** (Batch API 쓰면 절반). 캐시 적중 84%.
소분류 153종 목록이 프롬프트의 대부분이라, `cache_control` 없이 돌리면 몇 배가 된다.
**system 문자열에 장소명·날짜를 끼워 넣지 말 것** — 캐시는 접두사 일치라 통째로 무효가 된다.

### 🚨 YouTube 채널 분석에 `search` 를 쓰면 33배 비싸다 (2026-08-12)

```
search.list        100 units   ← 이것만 비싸다
videos.list          1         (id 50개 묶음)
playlistItems.list   1         (50개 묶음)
channels.list        1
commentThreads.list  1
```

채널 최근 영상을 `search` 로 찾으면 **100 units**, `channels.list` 로 업로드 재생목록을
받아 `playlistItems` 로 타면 **3 units** 다. 실측으로 영상 50편에 3 units 였다.

→ 승인 전 기본 쿼터 10,000 으로도 **채널 3,300개**를 돌려볼 수 있다.
   채널 분석 기능은 쿼터 상향을 기다릴 필요가 없다.

`npm run yt:channel -- @핸들` / `yt:search` / `yt:comments`. 소비량은 `yt_quota` 표에
날짜별로 남는다 — 프로세스가 기억하면 재실행마다 0 부터 세서 한도를 넘긴다.
**쿼터 리셋은 태평양 시간 자정**이다. 한국 시간이 아니다.

### 자막은 못 받는다 — 챕터가 대체재인데 12% 뿐이다 (실측)

`captions.download` 는 **영상 소유자만** 호출할 수 있다. 남의 영상 자막은 API 로 못 가져온다.
`timedtext` 를 긁는 우회는 약관 위반이라 감사에서 탈락한다.

→ 대신 설명란 타임스탬프를 챕터로 판다 (`parseChapters`). 요약과 달리 지명과 순서가 남는다.

⚠️ **다만 SPEC 이 잡았던 "절반쯤"이 아니다.** `한식 브이로그` 검색 50편 중 **6편(12%)** 뿐이었다.
   지역 여행 브이로그는 더 높을 수 있지만, 챕터에만 기대면 안 된다.
   → 영상→장소 연결은 **제목·설명 본문·댓글**이 주력이고 챕터는 보조다.

파서는 3개 이상 + 첫 항목이 60초 이내일 때만 인정한다. 안 그러면 "경기 3:1 승리" 가 챕터로 잡힌다.

### 🚨 영상 → 장소 연결을 실측했다. 설계를 바꿔야 한다 (2026-08-12)

`npm run eval:videoplace` — 실제 수집 영상 499편 대조.

이게 이 프로젝트에서 **가장 검증 안 된 가정**이었다. 서비스 전체가 "같은 소재의 영상이
얼마나 잘 됐나" 위에 서 있는데, 그러려면 영상이 어느 장소를 찍었는지 알아야 한다.

```
상호명으로 찾기        8.6%
지명(시군구)으로 찾기  22.6%   ← 2.6배

한국 지명이 잡힌 113편 중에서
  장소까지 특정   27.4%
  지역만 앎       72.6%   ← 대부분이 여기
```

**→ 장소 단위가 아니라 지역 단위가 기본이어야 한다.** 영상은 "곡성 오일장" 이라고 안 하고
"곡성 여행" 이라고 한다. 지역을 먼저 잡고 그 안에서 소재를 좁히는 순서로 가야 한다.

#### 라틴 문자 상호명은 반드시 걸러야 한다

첫 시도에서 11.8% 가 나왔는데 **대부분이 오탐**이었다. 남극 다큐가 서울 카페를 찍은 것으로
잡혔다 — TourAPI 음식점·카페에 `Scene`, `TINC`, `A.zel`, `Extraordinary` 같은 이름이 있고,
이게 영어 설명란의 평범한 문장에 걸린다.

거르고 나니 8.6% 로 내려갔지만 남은 건 전부 진짜다
(`묵호등대`, `광안리해수욕장`, `부평깡통시장`, `핫들생태공원`).

> **오탐이 미탐보다 나쁘다.** 틀린 지역에 성과를 몰아주면 추천이 조용히 망가진다.

#### 채널별로 갈린다 — 방법이 아니라 표본 문제였다

```
써머진(국내 여행 브이로그)    33%
Sol Life                     25%
영국남자·Her 86m2·Travel with AK   0%
```

0% 채널들은 한국 지역을 안 찍는다. **국내 여행 브이로그는 설명란에 코스를 나열한다** —
`공주여행 → 루치아의 뜰, 가가책방, 메타세콰이어길` 처럼 그대로 뽑힌다.

#### 그래서 검색어를 다시 짰다 (2026-08-12)

`npm run yt:search -- --dry` 로 **쿼터를 쓰지 않고** 계획을 먼저 본다.
1,000개 × 100 units 짜리 계획을 눈으로 확인하지 않고 돌릴 수는 없다.

```
지역  229 시군구 × 3패턴   "곡성 여행 브이로그" / "곡성 여행 코스" / "곡성 1박2일"
소재   66 종 × 1패턴       "5일장 여행 브이로그"
영어   30개                "Jeju Korea travel vlog" / "Korea abandoned school"
────────────────────────────────
696개 · 3페이지 · 208,800 units   (신청한 300,000 안)
```

세 가지가 바뀌었다:

1. **여행 맥락을 검색어에 박는다.** `여행`·`브이로그`·`코스` 가 없으면 먹방·숙소리뷰가 온다
2. **행정 접미사를 뗀다.** 영상은 "곡성군" 이라고 안 한다. `곡성` 으로 검색해야 걸린다
3. **인구감소지역을 앞에 둔다.** 쿼터가 중간에 마르면 뒤가 잘리는데,
   이 서비스가 답해야 하는 곳이 거기다

소재 검색에서 **음식점·숙박·레포츠·공연은 뺐다.** 그 이름으로 검색하면 목적지가 아니라
먹방·숙소리뷰·경기중계가 온다.

**검증**: `곡성 여행 브이로그` 로 50편만 더 받았는데 전 지표가 올랐다.

```
             이전    이후
장소 특정     8.6% → 12.4%
지명          22.6% → 29.6%
지명 중 장소  27.4% → 34.6%
```

받은 50편 중 14편이 설명란에 목록형 코스를 적어뒀다.
`곡성 맛집, 카페, 숙소, 북카페까지`, `곡성역, 섬진강 기차마을` — 우리가 원하던 그 데이터다.

### 채널 분석만 먼저 실데이터로 붙였다 (2026-08-12)

`npm run export:channels` → `src/data/real/channels.json` → `repo.ts` 가 읽는다.

화면에서 YouTube API 를 부르지 않는다(SPEC 6장). 배포된 사이트는 우리 SQLite 도 못 읽는다
(DB 는 커밋하지 않는다). → **빌드 시점 JSON 으로 굽는다. 방문자가 늘어도 쿼터는 0 이다.**

실채널 7개 수집 (채널당 3 units, 합 21 units):

| 채널 | 구독자 | 최근 성과 |
|---|---|---|
| 영국남자 | 620만 | 0.016× |
| Travel with AK | 197만 | 0.595× |
| Korea travel | 3.2만 | 0.939× |
| **은윤이행님** | **2,080** | **1.539×** |

**설계가 실측으로 확인됐다.** 조회수만 보면 620만 채널이 압도적인데, 채널 규모를 지우면
정반대다. SPEC 4장의 `조회수÷구독자` 가 실제 데이터에서 그대로 재현된다.

⚠️ **한 화면에 실측과 시연이 섞인다.** 구독자·편수·성과는 실측이지만 **소재 태그는 시연**이다
   (영상→장소→태그가 5단계). 화면이 그 사실을 직접 말하게 해뒀다 (`tagsAreDemo`).
   밝히지 않으면 크리에이터가 없는 근거를 믿고 움직인다.

### 공유 데이터 (공모전 제출용)

`npm run export:share` → `data/share/soom_places_declining.csv` (8.13MB / 제한 10MB)

제출 칸이 "공공데이터를 기반으로 **생성된** 데이터" 를 요구한다. 원본을 그대로 내면
점수가 안 나온다. → 지역코드 확보율을 **45.6% → 100%** 로 올린 것과, 그 덕에 처음
셀 수 있게 된 **인구감소지역 소재 관광지 12,518건**이 이 파일의 값어치다.

`지역코드_출처` 열을 반드시 남길 것. 받는 쪽이 원본만 걸러 쓸 수 있어야 한다.

### ⚠️ 게이트웨이 오류는 봉투가 다르다 — 이걸로 18,000건 헛돌았다 (2026-08-05)

```
정상 / 서비스 오류 : { response: { header: { resultCode, resultMsg } } }
게이트웨이 오류    : { OpenAPI_ServiceResponse: { cmmMsgHeader: { returnReasonCode, errMsg } } }
```

`response.header` 만 읽으면 게이트웨이 오류의 코드가 전부 `"?"` 가 된다.
그러면 호출자가 **쿼터 초과(22)를 구분하지 못해 한도를 넘긴 뒤에도 계속 두드린다.**
화면에는 "실패"로만 찍혀서 원인이 안 보인다. 실제로 18,119건을 그렇게 날렸다.

→ `scripts/lib/tourapi.ts` 에서 두 봉투를 모두 본다. 수집 스크립트에는
   **연속 실패 50회면 멈추는 안전장치**를 따로 둔다 (코드 파싱을 못 믿는 경우 대비).

→ 실패한 행은 `status='fail'` 로 남고, 재실행 시 **다시 받을 대상으로 잡힌다.**
   "안 받음"과 "받았는데 소개글이 없음(empty)"을 구분하지 않으면 영원히 빈 채로 남는다.

### 🚨 일일 한도는 **오퍼레이션당 1,000건** — 이게 일정을 결정한다 (2026-08-05 실측)

| 오퍼레이션 | 그날 호출 | 결과 |
|---|---|---|
| KorService2 `detailCommon2` | **999** | 차단 |
| EngService2 `detailCommon2` | **997** | 차단 |
| KorService2 `areaBasedList2` | 946 | 통과 (아슬아슬) |
| EngService2 `areaBasedList2` | 159 | 통과 |
| `galleryList1` | 62 | 통과 |

한도는 **서비스별·오퍼레이션별로 따로** 걸린다. 계정 전체 합산이 아니다.
그래서 KorService2 가 막힌 날에도 PhotoGallery·PhokoAwrd·Durunubi 는 멀쩡히 받아진다.
→ **한쪽이 막히면 다른 서비스 수집으로 갈아탈 것.** 그날을 통째로 버리지 않는다.

**그리고 이게 진짜 문제다.** `detailCommon2` 는 contentId 당 1호출이라:

```
국문 소개글 48,929건 ÷ 1,000/일 = 49일
영문 소개글 15,108건 ÷ 1,000/일 = 15일
detailIntro2(장날·운영시간)도 별도로 49일
```

**개발계정으로는 공모전 일정 안에 4단계에 못 간다.** 운영계정 전환이 선택이 아니라 필수다.
(운영계정은 보통 100,000/일 → 국문 소개글 하루면 끝난다)

→ 신청에 필요한 운영 URL: **https://gwanjetap.netlify.app** (아래 배포 절 참조)

### ⚠️ 전수 수집의 두 함정 — 실측 (2026-08-05)

**1. `areaCode` 로 나눠 돌면 절반이 사라진다.**

```
필터 없음 전체      48,929
콘텐츠 타입별 합계  48,929   ← 맞다
시도별 합계         22,298   ← 26,631건이 없다
```

`areacode` 가 빈 항목이 대량으로 있다. 주소와 좌표는 멀쩡한데 지역코드만 안 붙어 있다.
특히 **쇼핑은 5.8% 만 지역코드가 있다.** 그런데 오일장·재래시장이 쇼핑(38)이고
노포·국밥집이 음식점(39)이라, 지역으로 돌면 이 서비스의 주력 소재가 통째로 빠진다.

→ **콘텐츠 타입으로 나눠 돌 것.** 타입 하나당 최대 136페이지라 깊은 페이징 위험도 없다.
   빈 지역코드는 2단계에서 주소·좌표로 채운다.

**2. 영문 서비스는 콘텐츠 타입 코드가 다르다.**

국문 코드를 넣으면 **오류 없이 0건**이 온다. 이게 제일 위험하다 — 실패로 안 보인다.

| | 관광지 | 문화시설 | 축제 | 여행코스 | 레포츠 | 숙박 | 쇼핑 | 음식점 |
|---|---|---|---|---|---|---|---|---|
| 국문 | 12 | 14 | 15 | 25 | 28 | 32 | 38 | 39 |
| 영문 | 76 | 78 | 85 | 75 | 77 | 80 | 79 | 82 |

### 천문연 출몰시각 — 승인·실호출 확인 (2026-08-12)

```
https://apis.data.go.kr/B090041/openapi/service/RiseSetInfoService/getAreaRiseSetInfo
  ?serviceKey=...&locdate=20260813&location=서울      → resultCode 00
```

**기존 `DATA_GO_KR_API_KEY` 를 그대로 쓴다.** 계정당 인증키는 하나다.
서비스마다 키가 따로 나오는 게 아니라, 승인된 모든 서비스에 같은 키를 쓴다.

한 번 호출에 필요한 게 다 온다 (`0546` = 05:46 형식):

| 필드 | 뜻 | 쓰임 |
|---|---|---|
| `sunrise` / `sunset` | 일출 / 일몰 | S4 ⑤ 촬영 시간 |
| `civilm` / `civile` | 시민박명 시작 / 끝 | **골든아워** — 실제로 찍는 시간대 |
| `naute` / `aste` | 항해·천문박명 | 야경·별 촬영 |
| `moonrise` / `moonset` | 월출 / 월몰 | |

⚠️ `location` 은 지역명 문자열이다(`서울`). 좌표가 아니다.
   응답에 `latitudeNum`·`longitudeNum` 이 함께 오므로, 시도 17개만 받아두고
   장소 좌표에서 가장 가까운 지점을 쓰면 된다 (`geo.ts`).

⚠️ `getLCRiseSetInfo` 는 같은 파라미터로 **빈 배열**이 온다. 다른 오퍼레이션이니
   `getAreaRiseSetInfo` 를 쓸 것.

#### 수집 완료 — 주 1회만 받고 보간한다 (2026-08-12)

`npm run collect:sunrise` → `npm run export:suntime` → `src/lib/suntime.ts` 가 읽는다.

매일 받으면 20지점 × 365일 = **7,300회**라 개발계정으로 8일이 걸린다.
일출은 하루 1~2분씩 매끄럽게 움직이므로 **주 1회만 받고 사이를 선형 보간**했다.

```
20지점 × 53주 = 1,060건   한 번에 수집 완료 (실패 0)
```

**보간 오차를 실측했다** — 표본 사이 날짜 16개를 천문연 실제 값과 대조:

```
평균 0.1분 · 최대 1분
```

골든아워 안내에 차고 넘친다. 8일 걸릴 일이 한 번에 끝났다.

지점 20곳(서울·인천·수원·춘천·강릉·청주·세종·대전·홍성·안동·포항·대구·울산·부산·
창원·전주·광주·무안·목포·제주)은 전부 실호출로 확인했다. `sunTimeFor(lat,lng,날짜)` 가
장소 좌표에서 최근접 지점을 골라 쓴다.

⚠️ 화면에 **어느 지점 기준인지 밝힐 것.** 그 장소에서 직접 잰 값이 아니다
   (`SunTime.site`).

### TourAPI — 실측으로 확인된 것 (2026-08-03)

`npm run probe` 로 언제든 재확인 가능. 확인된 오퍼레이션명:

| 서비스 | 오퍼레이션 | 비고 |
|---|---|---|
| KorService2 | `areaCode2` | 시도 17건 |
| KorService2 | `areaBasedList2` | **전체 48,925건** |
| KorService2 | `searchKeyword2` | |
| KorService2 | `detailCommon2` | ⚠️ 아래 함정 참조 |
| KorService2 | `detailIntro2` / `detailInfo2` / `detailImage2` | |
| KorService2 | `searchFestival2` / `searchStay2` | 축제·숙박은 국문 서비스에 포함 |
| EngService2 | `areaBasedList2` | 전체 15,095건 (국문의 31%) |
| PhotoGalleryService1 | `galleryList1` | 6,118건 |
| PhokoAwrdService | `phokoAwrdList` | 95건 |
| Durunubi | `courseList` | 152건 |

| TarRlteTarService1 | `areaBasedList1` / `searchKeyword1` | 필수: baseYm, areaCd, signguCd |
| LocgoHubTarService1 | `areaBasedList1` | 필수: baseYm, areaCd, signguCd |
| AreaTarResDemService | `areaTarSvcDemList` / `areaCulResDemList` | 필수: baseYm, areaCd, signguCd, 지표코드 |
| AreaTarDivService | `areaTouDivList` / `areaExpDivList` / `areaIntlDivList` | 필수: baseYm, areaCd, signguCd, 지표코드 |

**⚠️ 최대 함정 — `NO_OPENAPI_SERVICE_ERROR(12)` 의 진짜 원인**

"해당 오픈API 서비스가 없거나 폐기됨" 이라고 나오지만 **오퍼레이션명 문제가 아닌 경우가 대부분이다.**
이 게이트웨이는 파라미터가 어긋나면 같은 오류를 뱉는다. 두 방향 다 걸린다.

1. **필수 파라미터가 빠졌을 때** — 매뉴얼의 `항목구분` 열이 `1` 이면 필수, `0` 이면 선택.
   `LocgoHubTarService1` 은 baseYm·areaCd·signguCd 셋 다 필수라 하나만 빠져도 거부당한다.
2. **없는 파라미터를 보냈을 때** — `detailCommon2` 에 `defaultYN`·`overviewYN`·`firstImageYN`·
   `contentTypeId` 를 붙이면 거부당한다. KorService2 에서 사라진 파라미터인데 블로그 예제엔 아직 있다.
   **`contentId` 하나만 보낼 것.**

→ 이 오류가 나면 오퍼레이션명을 의심하기 전에 **매뉴얼의 요청 파라미터 표부터 대조할 것.**
   활용매뉴얼(docx)은 각 서비스 data.go.kr 상세페이지의 참고문서에서 받는다.

### 소개글 확보율 (1단계 완료 판정)

표본 156건(오일장·전통시장 85 + 등대 71) 기준 **99%**. 평균 292~394자.
좌표 100%, 대표이미지 89~94%. → **LLM 태깅 계획대로 진행 가능.**

소개글 안에 장날이 텍스트로 들어있다 ("5일(4일, 9일)마다", "끝자리가 3, 8인 날").
전통시장 표준데이터 없이도 LLM 으로 뽑을 수 있을지 4단계에서 확인할 것.

검색어 설계가 결과를 크게 가른다. `오일장` 4건 vs `전통시장` 71건.

- TourAPI 오퍼레이션명에 숫자 접미사 주의 (`searchKeyword2` 등). 블로그 예제 신뢰 금지.
- 폐교 데이터는 상태 필터링 필수 (절반 이상 매각/철거됨)
- 중복 판정: 좌표 300m + 이름 유사도
- YouTube 수집은 체크포인트 구조로. 쿼터 소진 시 이어받기 가능해야 함.

### 5단계에서 같이 받아둘 것 — 영상 구성 (`VideoBreakdown`)

카드 펼침의 중심이 "잘 된 영상이 **어떻게 생겼나**" 라서, 조회수만 받아오면 화면이 빈다.
전부 실제로 얻을 수 있는 것만 쓰고 있다. 억지로 만들어 채우지 말 것.

| 필드 | 출처 |
|---|---|
| title · duration · view_count | YouTube API |
| `chapters` | **영상 설명란의 타임스탬프 목록을 정규식으로 파싱** |
| `hook` | 자막 앞 30초 → LLM 한 줄 |

여행 브이로그의 절반쯤은 설명란에 챕터를 적어 둔다 (`00:00 순창 / 08:12 남원` 형태).
없는 영상은 `chapters: []` 로 두면 화면이 그 블록을 안 그린다.
