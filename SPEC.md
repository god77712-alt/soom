# 숨 (SOOM) — 개발 명세서

> 2026 관광데이터 활용 공모전 출품작
> 크리에이터에게 "여기서 찍으면 잘 된다"를 데이터로 증명하는 촬영지 추천 서비스

---

## 0. 이 문서를 읽는 AI에게

이 문서는 **왜 이렇게 설계했는지**까지 담고 있다. 구현 중에 "이건 단순화해도 되지 않나" 싶은 부분이 나오면, 그 항목의 `이유` 를 먼저 읽을 것. 대부분 단순화하면 서비스 목적이 깨지는 장치들이다.

특히 다음 4가지는 **절대 임의로 바꾸지 말 것**:
1. 언어별 점수판 분리 (ko / en)
2. 태그 2단 구조와 폴백 규칙
3. 희소성 가중치 (영상 적은 곳 우대)
4. 근거 전면 공개 원칙

---

## 1. 서비스 한 줄 정의

**관광객을 보내는 서비스가 아니라, 관광객을 데려올 사람을 보내는 서비스.**

### 문제
- 사람들은 콘텐츠로 본 곳에 간다. 콘텐츠가 없는 지역엔 아무도 안 간다.
- 크리에이터는 안 알려진 곳을 안 간다. 조회수가 안 나올 리스크 때문에.
- 그래서 인구감소지역은 영원히 콘텐츠가 안 생긴다. **이게 진짜 병목.**

### 해결
크리에이터에게 "여기가 당신 채널에 유리하다"는 **데이터 근거**를 제공해 리스크를 제거한다.
크리에이터는 촬영 때문에 2~3일 체류하고, 그가 만든 영상이 후속 방문자를 부른다.

### 타겟
한국 여행 콘텐츠를 만드는 크리에이터 (국내/해외 모두). 해외 채널을 전면에 내세운다.

---

## 2. 데이터 소스

### 2-1. 한국관광공사 TourAPI (필수)

| 서비스 | 용도 | 수집 방식 |
|---|---|---|
| 국문 관광정보 | 장소 마스터 + **소개글(태그 원료)** | 1회 전수 수집 |
| 영문 관광정보 | 해외 채널용 장소명/설명 | 1회 전수 수집 |
| 행사·축제 조회 | 시기 정보 | 온디맨드 |
| 숙박 조회 | 체류 모듈 | 온디맨드 |
| 관광사진 갤러리 | 장소 이미지 | 1회 수집 |

> **주의**: 오퍼레이션명이 버전별로 다르다 (`searchKeyword2`, `areaBasedList2` 등 숫자 접미사).
> 반드시 발급받은 서비스의 실제 문서를 확인하고 사용할 것. 블로그 예제 그대로 쓰면 404.

### 2-2. 보강 공공데이터 (파일 수집, 1회)

| 데이터 | 범위 | 태그 부여 방식 |
|---|---|---|
| 전통시장 | 전량 (~1,400) | 규칙 기반 |
| 폐교재산 | **미매각/미활용만** (~500) | 규칙 기반 |
| 철도역 | **운영 중인 간이역·무인역만** (~100) | 규칙 기반 |

> 폐교의 절반 이상은 이미 매각/용도변경되어 현장에 없다. 반드시 상태 필터링할 것.

### 2-3. 기능용 API

- **YouTube Data API v3** — 검색, 영상 통계, 채널 통계, **댓글**
- **카카오맵** — 지도 표시, 주소→좌표 변환(중복 판정용), 이동 시간
- **한국천문연구원 출몰시각** — 일출/일몰 시각 (촬영 시간 추천의 근거)
- **LLM** — 소개글→태그 추출, 촬영 구성안 생성

---

## 3. 데이터 모델

### places (장소 마스터)

```
id                  PK
source              tourapi | market | school | station
source_id           원본 ID
name_ko             한글명
name_en             영문명 (없으면 로마자 변환)
description_ko      소개글 (태그 추출 원료)
description_en      영문 소개
sido / sigungu      행정구역
sigungu_code        시군구 코드
lat / lng           좌표
is_declining_area   인구감소지역 여부 (bool)
image_url           대표 이미지
content_type_id     TourAPI 분류
data_reliability    high(tourapi) | medium(market) | low(school,station)
created_at
```

> `data_reliability` 가 low 인 장소는 화면에 **"공공데이터 기준, 현장 확인 권장"** 문구를 노출한다.

### tags (2단 태그)

```
id                  PK
code                slug (예: oil_market)
name_ko             오일장
name_en             Five-day market
parent_id           상위 태그 (NULL이면 대분류)
level               1(대분류, 20개) | 2(세부, ~150개)
is_seasonal         계절 태그 여부
season_months       [4,5] 형태. 상시면 NULL
```

**대분류 20개 예시**: 시장·상권 / 폐허·근대유산 / 교통·역 / 바다·해안 / 산·계곡 / 논밭·농촌 / 사찰·종교 / 골목·주거 / 야경·조명 / 계절식생 / 축제·행사 / 음식·식당 / 노동현장 / 섬 / 하천·호수 / 목욕·생활 / 공방·장인 / 동물 / 스포츠·레저 / 기타

**세부 태그 예시**: 오일장, 수산시장, 상설시장, 야시장, 폐교, 폐공장, 폐선로, 간이역, 무인역, 등대, 방파제, 염전, 계단식논, 돌담길, 대나무숲, 새벽물안개, 억새, 벚꽃, 눈꽃, 단풍, 노포, 목욕탕, 국밥집 …

### place_tags

```
place_id
tag_id
confidence          0.0~1.0 (LLM 추출 신뢰도)
method              llm | rule
```

> 같은 장소가 여러 계절 태그를 가질 수 있다. (진해 어느 길 = 벚꽃 + 가로수길)

### videos

```
id                  PK
youtube_id
channel_id
title / description
published_at
view_count
language            ko | en   ← 반드시 분리
duration
```

**수집 조건**: `published_at` 이 3년 이내인 것만. 그 이상은 저장하지 않는다.

### channels

```
id
youtube_channel_id
title
subscriber_count
sub_band            1(1k~1만) | 2(1만~10만) | 3(10만~100만) | 4(100만+)
language            ko | en
```

**구독자 1,000 미만 채널의 영상은 점수 계산에서 제외한다.** (배수가 폭발해서 통계를 망침)

### video_places (영상 ↔ 장소 매핑)

```
video_id
place_id
weight              1.0 | 0.5 | 0.2
evidence            title | description | timestamp | comment
```

**가중치 규칙 (중요)**
| 등장 위치 | weight |
|---|---|
| 제목에 장소명 등장 | 1.0 |
| 설명란 / 타임스탬프 목록에 등장 | 0.5 |
| 단순 언급, 댓글에서만 확인 | 0.2 |

> **이유**: 여행 브이로그 한 편에 장소가 5~6곳 나온다. 조회수를 전부에게 100% 주면 스쳐 지나간 곳이 대박 명소가 되어 데이터가 오염된다. 안 주면 여행 영상 대부분을 못 쓴다.

**댓글 보너스**: 해당 장소를 언급한 댓글 중 좋아요 상위 댓글이 있거나, "여기 어디예요" 류 질문이 3건 이상이면 weight 에 +0.3 (최대 1.0). 그 장면이 먹혔다는 신호.

### video_tags

```
video_id
tag_id
weight              해당 장소들의 weight 승계
```

### tag_scores (핵심 테이블)

```
tag_id
language            ko | en      ← 반드시 분리
sub_band            1~4
video_count         표본 수
median_vsr          조회수/구독자 중앙값
p25_vsr / p75_vsr   범위 표시용
is_fallback         상위 태그 점수를 빌려온 경우 true
computed_at
```

### recommendation_logs

```
channel_id
place_id
tag_id
shown_at
selected_tag_id     사용자가 추가로 고른 태그
```

> 지금은 기록만. 나중에 "비슷한 채널이 많이 고른 태그" 기능의 재료가 된다. 발전성 근거.

---

## 4. 점수 계산 규칙

### 4-1. 영상 성과 지표

```
VSR = view_count / subscriber_count
```

- 3년 이내 영상만
- 구독자 1,000 미만 채널 제외
- 평균이 아니라 **중앙값(median)** 사용 (이상치 방어)

### 4-2. 태그 성과 점수

```
tag_score[tag, lang, band] = median(VSR of videos tagged with `tag`)
```

**폴백 규칙 (필수)**
```
if video_count < 5:
    상위(level 1) 태그의 점수를 사용하고 is_fallback = true
if 상위 태그도 < 5:
    점수 없음. 화면에 "표본 부족" 표시
```

> **이유**: 3년으로 자르면 희귀 태그가 2~3편만 남는다. 그 2편이 우연히 대박이면 그 태그가 1위가 된다. 2단 태그 구조는 정확히 이 문제를 위해 존재한다.

### 4-3. 숨 스코어 (장소 순위)

```
scarcity   = 1 / log(1 + place_video_count[place, lang] + 1)
soom_score = tag_score[tag, lang, band] × scarcity
```

- `place_video_count` 는 **언어별로 따로 센다.** 국내 영상 34편 / 해외 영상 0편인 곳이 핵심 타겟.
- 인구감소지역에 인위적 가산점을 주지 않는다. 희소성 가중치만으로 자연히 상위에 올라온다.

> **이유**: "우리가 인구감소지역을 편애했다"가 아니라 "데이터가 그렇게 말했다"는 구조여야 심사에서 논리가 선다.

### 4-4. 예상 도달 범위

```
low  = subscriber_count × p25_vsr
high = subscriber_count × p75_vsr
```

**반드시 범위로 표시. 단일 숫자 금지.**
표시 옆에 항상: `과거 영상 데이터 기반 추정입니다. 결과는 콘텐츠 완성도에 따라 달라집니다.`

### 4-5. 채널 분석

```
1. 최근 50편 수집
2. 각 영상의 VSR 계산
3. VSR 상위 영상에 가중치 부여 (평균 색깔이 아닌 "잘 되는 색깔"을 찾는다)
4. 가중 집계로 상위 태그 3~5개 추출 → 채널 프로필
```

**한국 영상이 없는 해외 채널**: 다른 나라 영상에서 뽑은 태그를 그대로 사용한다.
태국 야시장 찍는 사람은 한국 오면 오일장을 찍는다. 태그는 국경을 안 가린다.

---

## 5. 화면 명세

### S1. 온보딩

- 유튜브 채널 URL 입력 (단일 입력창)
- URL 없이 들어온 사용자를 위한 대체: 태그 3개 직접 선택
- **국적을 묻지 않는다.** 영상 언어로 자동 판별.

### S2. 채널 프로필 — "당신 채널의 색깔"

```
@channelname · 구독자 2.1만 · 주 언어 English

당신 채널의 색깔
[재래시장] [노포·식당] [지하철·대중교통]

근거: 최근 50편 중 상위 성과 영상 12편에서 공통 추출
```

> **이유**: 추천 5곳이 안 맞아도 분석이 맞으면 신뢰가 유지된다.
> 바로 추천부터 던지면 안 맞을 때 그냥 이탈한다. 발표에서도 이 화면이 가장 잘 먹힌다.

### S3. 추천 결과 (5곳)

- **가장 강한 태그 하나로 5곳을 채운다.** 여러 태그를 섞지 않는다. (근거가 선명해야 함)
- 각 카드 필수 요소:

```
순창 오일장 · 전북 순창              [인구감소지역]
🇰🇷 국내 채널  1.2배 · 영상 34편
🌏 해외 채널  4.1배 · 영상 2편        ← 미개척
서울에서 3시간 20분
```

- 한쪽 데이터가 없을 때 **"데이터 없음"이라고 쓰지 말 것.**
  → `해외 채널 촬영 기록 없음 — 미개척` + 강조색 처리.
  "없음"으로 표시하면 사용자가 좋은 후보를 스스로 걸러낸다.

**하단: 태그 확장 영역**
```
다른 태그로도 찾아보시겠어요?
[야시장 NOW] [수산시장] [상설시장] [노포] [시장골목] [국밥집] [상인] [새벽시장]
[등대] [폐교]   ← 전혀 다른 대분류에서 2개 섞어 넣기
```
- 형제 태그(같은 level 1 하위) 8개 + **탐색용 다른 대분류 태그 2개**
- 계절 태그 표시:
  - 상시 → 기본색
  - 계절, 지금 시즌 → 강조색 + `NOW`
  - 계절, 시즌 아님 → 흐린색 + `10월부터`
- **계절 태그를 숨기지 않는다.** 크리에이터는 미리 준비하는 사람들이다.

### S4. 장소 상세 — 확신을 만드는 6단 구조

**순서를 바꾸지 말 것. 의심을 하나씩 걷어내는 순서다.**

```
① 이 소재는 먹힌다
   오일장 소재 영상 47편 · 구독자 대비 중앙값 3.2배
   [썸네일] 곡성 오일장 · 1.2만 → 41만 (34배)
   [썸네일] 화개장터   · 8만  → 62만 (7.7배)
   [썸네일] 정선 5일장 · 3만  → 19만 (6.3배)
   → 유튜브 임베드로 바로 재생 가능하게

② 그런데 여긴 비어 있다
   순창 오일장 관련 영상 (전체)
   [썸네일] 3년 전 · 구독자 500 → 1.2만
   → 위아래 대비를 눈으로 보여준다. 문장 열 줄보다 강하다.

③ 별로라서가 아니다
   · 관광공사 등록 정보 있음 · 사진 8장 · 접근 가능
   · 비슷한 규모 오일장 평균 영상 14편, 여긴 2편
   → 크리에이터의 첫 의심은 "별로라서 아무도 안 간 거 아냐?"다.
      이걸 안 풀면 확신이 안 생긴다. 절대 생략 금지.

④ 당신이면 이 정도
   구독자 2.1만 기준 예상 도달 3만 ~ 9만
   (과거 데이터 기반 추정입니다)

⑤ 이렇게 찍으면 된다
   10월 12일 일출 06:41 → 장 서는 모습 → 상인 → 국밥
   "잘 된 영상 47편의 공통 구성"이라고 반드시 근거를 밝힐 것
   제목 예시: "The Korean market tourists never find"

⑥ 이렇게 머물면 된다
   숙소 3곳 (TourAPI) · 주변 축제 · 예상 동선
```

> ⑤에서 AI가 지어낸 것처럼 보이면 앞의 데이터까지 신뢰를 잃는다.
> 반드시 "N편에서 추출한 공통 패턴"이라는 출처를 함께 표시할 것.

### S5. 어드민 콘솔 (기관용)

같은 엔진, 다른 뷰. 관광공사·지자체 담당자 시점.

- 시군구별 미개척 소재 랭킹
- 채널 ↔ 지역 매칭 추천 (팸투어 대상자 선정용)
- 누적 추천 → 예상 체류일수 → 생활인구 환산 대시보드
- 성공 지표: **인구감소지역에서 신규 생성된 영상 수**

> "실제로 쓸 수 있냐"는 질문에 대한 답이자 발전성 20점의 근거.

---

## 6. 아키텍처 원칙

```
[사전 배치 — DB에 미리 적재]
  TourAPI 관광지 ─┐
  전통시장 ───────┤
  폐교 ──────────┼→ 중복정리 → LLM 태깅 → places (~4만)
  철도역 ────────┘
  YouTube 영상+댓글 → 태그 매핑 → tag_scores (언어별)

[온디맨드 — 사용자 요청 시에만]
  TourAPI 축제/숙박 · 카카오맵 · 일출시각 · LLM 구성안
```

### 절대 원칙

1. **화면은 YouTube API를 직접 호출하지 않는다.**
   사용자 액션마다 유튜브를 부르면 시연 중 쿼터 소진으로 서비스가 멈춘다.
   모든 조회는 자체 DB에서.
2. 전통시장/폐교/철도역은 API 연결이 아니라 **1회 파일 적재**.
   외부 서버 장애가 우리 서비스를 죽이지 않게 한다.
3. LLM 태깅은 전량 배치. 실시간 태깅 금지.

### 중복 판정 규칙

```
좌표 300m 이내 AND 이름 유사도 임계 이상 → 동일 장소로 병합
우선순위: tourapi > market > school > station
```

> 정선 5일장 등 유명 시장은 TourAPI와 전통시장 데이터 양쪽에 존재한다.
> 병합하지 않으면 영상 수가 갈려 점수가 반토막 난다.

---

## 7. 구축 순서

**한 번에 다 만들지 말 것. 단계마다 눈으로 확인하고 넘어간다.**

| 단계 | 작업 | 완료 판정 |
|---|---|---|
| 0 | 가짜 데이터 30건으로 화면 5개 뼈대 | API 없이 화면이 돈다 |
| 1 | TourAPI 전수 수집 | **소개글 채워진 비율 확인** ← 여기가 낮으면 계획 수정 |
| 2 | 전통시장/폐교/철도역 적재 + 중복 정리 | 병합 결과 30건 육안 확인 |
| 3 | 태그 체계 확정 (대분류 20 + 세부 150) | 실제 데이터 100건 태깅해보고 확정 |
| 4 | LLM 전량 태깅 | 태그별 장소 수 분포 확인 |
| 5 | YouTube 수집 (2주, 1.5만편+, 댓글 포함) | 언어별 균형 확인 |
| 6 | 점수 계산 | **상위 30개 육안 검증** ← "왜 이게 1위지?"가 나오면 4-2 규칙 재확인 |
| 7 | 화면에 실데이터 연결 | |
| 8 | 실제 크리에이터 3~5명 테스트 | 실용성 점수의 유일한 증거 |

**3단계는 머리로 미리 정하면 100% 틀린다.** 반드시 실제 소개글 100건을 보고 태그를 정할 것.

---

## 8. 유튜브 수집 전략

### 쿼터 관리
- search 호출이 가장 비싸다. 검색어를 신중하게 설계할 것.
- 목표: 3년 이내 영상 15,000편+ (ko/en 각각)
- 2주에 걸쳐 분산 실행. 하루 한도 소진 시 이어받기 가능한 체크포인트 구조로.

### 검색어 설계
```
ko: "{지역명} 여행", "{지역명} 브이로그", "{소재} 브이로그"
en: "Korea {소재} vlog", "hidden Korea", "Korea countryside", "{지역명} Korea"
```

### 댓글이 핵심인 이유
해외 채널 영상은 제목에 지명이 없다. "I Visited Korea's Most Beautiful Village" 식이다.
→ 댓글에 한국인들이 "여기 OO 아니에요?" 하고 달아준다. 여기서 지명을 확보한다.
→ 좋아요 많은 댓글이 특정 장면을 언급하면 그 장면이 먹혔다는 신호다.

> 재생 구간별 시청 그래프는 채널 소유자만 접근 가능하므로 사용 불가. 댓글이 그 대체재다.

---

## 9. 표시 원칙 (신뢰 관리)

| 상황 | 표시 |
|---|---|
| 예상 도달 | 반드시 범위 + 추정 고지 |
| 표본 부족 태그 | "표본 부족" 명시, 억지 점수 금지 |
| 폴백 점수 사용 | "상위 소재 기준" 표시 |
| 저신뢰 데이터 (폐교/간이역) | "공공데이터 기준, 현장 확인 권장" |
| 미개척 장소 | "없음"이 아니라 **"미개척"** + 강조색 |

크리에이터는 조회수 수치에 예민하다. 과장하는 순간 신뢰를 잃는다.
**한계를 솔직히 밝히는 쪽이 오히려 믿음직해 보이고, 심사에서도 그렇다.**

---

## 10. 심사 기준 대응

| 항목 | 배점 | 대응 |
|---|---|---|
| 서비스 구현성 | 30 | 화면 5개 완결, 사전 배치로 구동 안정성 확보 |
| 서비스 기획력 | 30 | "관광객을 데려올 사람을 보낸다" 논리 + 희소성 가중치 |
| 데이터 활용 적절성 | 20 | TourAPI 소개글을 원료로 **새 데이터셋(태그) 생성** |
| 서비스 발전성 | 20 | 어드민 콘솔 + 다국어 확장 구조 + 선택 로그 축적 |

**본선**: 적정성(논리성) 30 / 완성도 30 / 실용성 25 / 발표 15
→ 논리성이 최상단. 추천 결과보다 **추천 근거 화면(S4)**이 점수다.

---

## 11. 금지 사항

- YouTube 영상 통계를 무기한 저장하지 않는다 (API 약관 확인 필요)
- 영상은 임베드로만 표시. 다운로드/재호스팅 금지
- 예상 조회수를 단일 숫자로 표기 금지
- 사용자에게 국적을 직접 묻지 않는다
- 인구감소지역에 인위적 가산점 금지 (희소성 가중치로만)
- 특정 지역 추천 횟수 제한/감점 금지 (서비스 논리를 흐림)
