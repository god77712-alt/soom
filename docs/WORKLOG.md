# 작업 로그

**마지막 갱신 2026-08-22 17:16** · 작업일 8일 · 커밋 82건

> `npm run worklog` 으로 커밋 이력에서 다시 굽는다. **이 파일을 직접 고치지 말 것** —
> 다음 실행에서 덮어쓴다. 남길 말은 커밋 메시지에 적으면 여기 실린다.

---

## 2026-08-22  <sub>00:07~17:16 · 12건</sub>

- **현관에 지도를 되돌리고, 화면 말투를 구어체로 바꿨다** <sub>17:16 · `763fa41`</sub>
  두 가지다.
- **작업 로그 갱신** <sub>15:25 · `017ccfa`</sub>
- **지역 검색 100건을 더 받아 영상-장소 연결을 늘렸다** <sub>15:25 · `4c1ea61`</sub>
  TourAPI 는 오늘 detailCommon2·detailImage2·detailIntro2 세 버킷이 모두 [22] 로 막혀 있었고 detailInfo2·areaBasedList2 만 열려 있었다. 반면 YouTube 쿼터는 08-18 이후 안 써서 10,000 이 통째로 남아 있었다 — 그래서 그쪽에 썼다.
- **목록 카드에서 상세 링크를 뺐다 — 프로덕션에서 404 였다** <sub>01:10 · `726edad`</sub>
  카드 이름을 `/place/[id]` 로 걸었는데 배포하고 눌러 보니 404 였다. `/place/[id]` 는 **채널이 있어야만** 실데이터 상세를 그린다 (`realPlaceDetail(id, channel, hint)`). 채널 없이 들어오면 시연 경로로 내려가는데 거긴 카탈로그 id(`t132783`)를 모른다.
- **작업 로그 갱신** <sub>00:58 · `a4c57a0`</sub>
- **카드 완성도 리포트 + 화면에서 중복을 걷어냈다** <sub>00:58 · `47152de`</sub>
  `report:completeness` 는 **화면이 실제로 받는 것**을 센다 (`places.json`). `report:inventory` 는 DB 안의 장소를 세는데, 카탈로그는 소재당 240곳만 담고 담을 때 사진 있는 것을 앞으로 보내므로 둘이 다르다 — DB 사진 48% 인 상설시장이 카드에서는 76% 다. 크리에이터가 보는 건 후자다.
- **작업 로그 갱신** <sub>00:37 · `63f5953`</sub>
- **현관을 소재 목록으로 바꿨다 + 목록 화면이 얼어붙던 것을 고쳤다** <sub>00:37 · `218964e`</sub>
  예전 현관은 유튜브 채널 URL 을 넣기 전에는 아무것도 안 보여줬다. 처음 온 사람은 자기 채널 주소를 복사해 오거나 그냥 나간다. 그런데 채널 없이 쓰는 길이 페이지에서 시각적으로 제일 약한 요소였다 — 회색 작은 글씨 링크 하나 (`소재만 골라서 찾기`). 정작 그쪽이 12개 소재 6,309곳 전부 실데이터고 사진도 붙어 있다. **제일 좋은 걸 제일 안 보이게 두고 있었다.**
- **카드 재료를 카탈로그에 실었다 — 사진 여러 장·운영정보·대표 키워드** <sub>00:22 · `8c1b27c`</sub>
  순위로 장소를 가를 근거는 없다(3항). 차이는 카드 내용으로 낸다고 정해 놓고도 카드가 쓸 수 있는 건 이름·지역·인구감소 배지뿐이었다. 오늘 받은 운영정보 998건과 사진 5,077장을 카드가 읽는 자리까지 연결했다.
- **운영정보 수집기(detailIntro2) — 또 하나의 안 쓰던 1,000/일 버킷** <sub>00:14 · `357f83d`</sub>
  운영시간·휴무일·주차·이용요금·특산물. 카드 내용의 재료다. 순위로 장소를 가를 근거는 우리에게 없으니(3항) 차이는 카드 내용으로 내야 하는데, 지금 카드가 쓸 수 있는 건 볼거리 933곳뿐이었다.
- **작업 로그 갱신** <sub>00:07 · `2461eb4`</sub>
- **사진 수집기(detailImage2) + 오퍼레이션별 한도 확인 명령** <sub>00:07 · `ccb382b`</sub>
  detailImage2 는 detailCommon2 와 **별도의 1,000/일 버킷**인데 한 번도 안 쓰고 있었다. 소개글이 [22] 로 막힌 날에도 사진은 그대로 받아진다 — 그날을 통째로 버리고 있었던 셈이다. 첫 실행에서 810곳 5,077장을 받았다 (실패 0).

## 2026-08-21  <sub>23:50~23:50 · 2건</sub>

- **작업 로그 갱신** <sub>23:50 · `d1c3dc4`</sub>
- **평가 스크립트가 matcher 사본을 들고 있었다 — 오탐을 그대로 세고 있었다** <sub>23:50 · `9b98e21`</sub>
  `eval-video-place.ts` 가 `lib/videoplace.ts` 의 matcher 를 통째로 복사해서 들고 있었다. 그래서 실제 적재(`build-video-place.ts`)에는 있는 자기 지역 관문 `mentionsOwnRegion` 이 평가에는 없었고, **평가만 오탐을 세고 있었다.** `제주여행`(포천시 업소)이 제주 영상마다 붙은 채로 리포트에 찍혔다. 오류는 안 뜬다 — 숫자가 조용히 틀릴 뿐이다. `canShowMultiplier`·`SUBJECT_PLAN` 과 같은 원칙: 같은 판단이 두 곳에 있으면 반드시 어긋난다.

## 2026-08-17  <sub>13:04~16:39 · 11건</sub>

- **작업 로그를 커밋 이력에서 굽는다 + 오늘 수집분** <sub>16:39 · `717ae22`</sub>
  npm run worklog → docs/WORKLOG.md (날짜별 · 마지막 갱신 일시 · 커밋 67건)
- **오늘 작업 정리 + 방향 재설정** <sub>16:17 · `439baf7`</sub>
  수집은 넘치고(영상 82,818 · 장소 50,033) 파는 것은 미완성이다. 소개글 10% · 폐교 사진 0% · 크리에이터 반응 0건.
- **문서 말투를 사용자 말투로 — README·한 장 요약** <sub>16:04 · `ffe16c3`</sub>
  보고서 문체라 남이 쓴 것처럼 읽혔다. 뜻은 그대로 두고 말만 우리가 쓰는 말로 바꿨다. 숫자·표·기능 명세는 손대지 않았다 — 심사에서 읽는 부분이라 건조한 게 맞다.
- **「같이 일할 때 지켜야 하는 것」 → 「지켜야 하는 기준」** <sub>15:09 · `2ad9715`</sub>
  훈계처럼 읽혔다. 그리고 금지 목록을 나열하는 대신, 각 줄마다 그 자리에서 실제로 있었던 일을 옆에 붙였다 — 왜 그 기준이 생겼는지가 근거다.
- **README 도 같은 구조로 — 개요·과제·해결·기능명세** <sub>15:03 · `43bebb0`</sub>
  한 장 요약만 고치고 저장소 첫 화면은 옛 구조 그대로 뒀었다. 두 문서가 다른 말을 하면 새로 오는 사람이 어느 쪽을 믿어야 할지 모른다.
- **한 장 요약을 문서 구조로 다시 짰다 — 개요·과제·해결·기능명세** <sub>14:40 · `f457875`</sub>
  전에 낸 두 판은 발견한 사실을 나열한 것이지 문서가 아니었다. 읽는 사람이 "무슨 과제를 왜 이렇게 푸는가" 를 순서대로 따라갈 수 없었다.
- **한 장 요약을 다시 만들었다 — 7.6pt 로 눌러 담아 읽히지 않았다** <sub>14:37 · `675377f`</sub>
  한 장에 다 넣으려고 글자를 줄이고 표·박스를 빽빽하게 붙였더니, 정작 한 장짜리 요약이 해야 할 일(30초 안에 뭘 하는 프로젝트인지 알린다)을 못 했다.
- **README — DB 는 Releases 에서 받는다** <sub>14:28 · `d53376c`</sub>
  143MB 라 커밋하지 않는다. 화면만 볼 거면 필요 없고(JSON 은 저장소에 있다), 가공·점수 스크립트를 돌릴 때만 필요하다. 며칠치를 다시 수집하지 않아도 되게 스냅샷 받는 법을 적었다.
- **README — 공개 저장소 첫 화면** <sub>13:52 · `44bf5b0`</sub>
  들어오면 파일 목록만 보였다. 새로 오는 사람이 30초 안에 알아야 하는 것만 담았다: 무엇을 푸는가 · 가설 검정 결과(기각된 것 포함) · 돌리는 순서 · 지켜야 할 6가지.
- **프로젝트 한 장 요약 — 새로 합류하는 사람용** <sub>13:27 · `ac6e7bd`</sub>
  과제·문제·해결·검정 결과·설명 원칙을 A4 한 장에 담았다. 숫자는 전부 현재 DB 실측이고, 기각된 가설(빈 곳이 기회다)도 그대로 적었다 — 숨기면 이 발표의 신뢰가 사라진다.
- **영어 소재 코퍼스를 채웠다 — 검색어 표가 두 벌이라 그동안 0편이었다** <sub>13:04 · `d158b3b`</sub>
  영어 채널을 넣으면 `순위만 · 수집 1편` 이 뜨던 이유가 SUBJECT_PLAN 에 영어 검색어가 없어서였다. 12개 주력 소재의 영어판을 짜서 6페이지씩 받았다 (3,450편 · 7,200 units).

## 2026-08-13  <sub>00:06~14:38 · 12건</sub>

- **오늘 작업 정리 + 내일 할 일** <sub>14:38 · `b9ca2ff`</sub>
  배포 완료. /check 는 ⑥번만 실패 — export 층에 남은 가산점을 정확히 잡고 있다.
- **지어낸 순위 신호를 되돌렸다 — 40km 도 0.35 도 근거가 없었다** <sub>14:29 · `2587f1b`</sub>
  인구감소 정렬 가산점을 뺀 자리를 `근처 40km 의 검증된 소재 수 × log1p × 0.35` 로 메웠는데, 40km 도 0.35 도 내가 만들어낸 숫자였다. **순위를 그럴듯하게 만들려고 지어낸 값은 점수를 지어내는 것과 다르지 않다.**
- **전체 검토 — 판정이 세 군데로 갈라져 있었고 두 경로가 정반대였다** <sub>14:16 · `cbabf66`</sub>
  ## 1. 배수를 그릴 자격을 한 곳으로 모았다
- **CLAUDE.md — 시연 데이터 제거 완료 · 기관 화면 원칙 추가** <sub>12:57 · `726e480`</sub>
- **/admin 실데이터화 — 못 재는 것은 숫자로 그리지 않는다** <sub>12:56 · `515a3cd`</sub>
  마지막 남은 시연 화면이었다. 시연 KPI 는 이랬다:
- **LLM 태그를 점수에 연결 — 돈 주고 산 표본이 안 쓰이고 있었다** <sub>12:44 · `a4fc585`</sub>
  tag:channel 로 1,814건을 붙여놨는데 export:tagscores 가 그걸 안 읽었다. 검색 표본(found_by like 'search:%')만 보고 있어서, 배수를 그릴 수 있는 칸이 17/79 그대로였다. 쓴 돈이 화면에 하나도 반영이 안 된 상태였다.
- **재제출 발송 완료 — 회신 대기로 상태 갱신** <sub>12:04 · `7f683c5`</sub>
- **YouTube 감사 재제출 — 유튜브가 나오는 화면에서 시연 데이터를 걷어냈다** <sub>11:45 · `7d01186`</sub>
  반려 사유는 문안이 아니라 형식이었다. 1차에 낸 건 정적 스크린샷 7장이라 "이걸 쓰면 무슨 일이 벌어지는가"가 순서대로 안 보였고, 화면이 전부 한국어라 심사자가 뭘 보고 있는지 알 방법이 없었다.
- **--all 안전장치 + 오늘 작업 정리** <sub>01:32 · `fcac2b6`</sub>
  ## --limit 없이는 안 돈다
- **채널 소재 태그를 실데이터로 — 다만 표본 자체가 틀렸었다** <sub>01:01 · `5862db3`</sub>
  계곡은개골개골  계곡 34 · 야영장 6 · 온천 6 · 폭포 5 · 스쿠버 3   오지는 오진다   고택 19 · 공원 4 · 한옥 2   하이갱스       한식 47 · 맛코스 40 · 상설시장 9
- **홈 실데이터화 2/2 — 추천 카드가 실장소로 바뀌었다** <sub>00:21 · `869a5ac`</sub>
  12개 주력 소재를 고르면 실장소·실영상·실장날로 카드가 나간다. 나머지 소재는 아직 시연이고, 화면이 소재 단위로 그 사실을 밝힌다.
- **홈 실데이터화 1/2 — 영상→장소 연결과 실제 태그 점수** <sub>00:06 · `936716b`</sub>
  홈 카드를 실장소로 바꾸려면 막혀 있던 것부터 풀어야 했다. 영상→장소 연결이 없으면 "경쟁 영상 0편"이 거짓말이 된다.

## 2026-08-12  <sub>10:55~23:54 · 27건</sub>

- **CLAUDE.md 모순 정리 — 실측이 뒤집은 규칙 반영** <sub>23:54 · `d402e4d`</sub>
  지침 파일에 모순이 남아 있으면 다음 세션이 옛 규칙을 따라 되돌린다.
- **목록 화면 — 실데이터로 도는 첫 화면** <sub>23:53 · `e8ec4f4`</sub>
  /start           12개 소재 인덱스 /subject/[slug]  소재별 촬영지 목록 + 시도 칩 + 장날
- **소재 x 지역 격자 실측 — 시군구 필터는 절반이 빈다** <sub>23:21 · `2d440f3`</sub>
  목록 화면을 만들기 전에 "빈 목록이 얼마나 나오는지"부터 쟀다. 전국 합계만 보면 넉넉해 보이는데 지역으로 좁히는 순간 무너진다.
- **목록 중심으로 전환 + 뽑아보니 드러난 무증상 실패 두 건** <sub>23:07 · `2bec04b`</sub>
  어필을 예측에서 목록으로 옮긴다 (사용자 지시).   (X) 여기 가면 잘 된다   (O) 이 소재를 찍을 수 있는 곳은 이만큼 있다. 영상이 적던 곳도 선택지가 된다 예측은 채널이 74%를 설명하고 개별 소재 쌍은 BH 보정을 하나도 통과 못 한다. 목록은 근거가 필요 없다 - 있으면 있는 것이다.
- **가설 A 검증 완료 — 소재는 성과를 가른다 (p=0.0007). 계산식 2곳 수정** <sub>22:56 · `e061bc7`</sub>
  ## 수집: 소재당 300편 (7,200 units, 하루 쿼터 안)
- **핵심 가설 2개를 처음으로 측정 + 날짜 박힌 촬영 계획** <sub>22:39 · `cbb9d4b`</sub>
  ## 1. 채널 메타 155개 (4 units)
- **쇼츠 후보 구간의 근거를 길이에서 댓글 반응으로 바꿈** <sub>21:45 · `68a61aa`</sub>
  npm run yt:comments  (474편 · 674 units)  -> 댓글 21,867건 npm run build:hotspots                    -> src/data/real/hotspots.json
- **채널 성적을 롱폼·쇼츠로 분리 + 롱폼에서 쇼츠 뽑을 구간 제안** <sub>21:15 · `d976f3a`</sub>
  ## 왜
- **홈 디자인 3건: 인천 관문 노선 · 채널 지표 · 부드러운 이동** <sub>20:33 · `b3154b3`</sub>
  1) 인천 관문 노선 지도가 목적지를 최근접 공항에 붙이는 방식이라, 목적지가 남쪽에 몰리면 김해에서만 퍼지는 그림이 됐다. 실제로는 해외 크리에이터 대부분이 인천으로 들어오므로 인천에서 전국으로 뻗는 층을 한 겹 더 깔았다. 금색(최근접)과 다른 청록을 쓴다 - 같은 색으로 겹치면 노선이 두 배로 늘어난 것처럼 보여 어느 게 어느 건지 못 읽는다.
- **출몰시각 수집 완료 (S4 ⑤ 근거)** <sub>20:06 · `f47f5b4`</sub>
  천문연 20지점 × 주 1회 = 1,060건. 한 번에 다 받았다 (실패 0).
- **천문연 출몰시각 승인 확인 + 실호출 검증** <sub>19:44 · `a57b315`</sub>
  신청 상태가 아니라 이미 승인돼 있었다. 실제로 불러서 확인했다.
- **신청 현황 정정: 천문연은 이미 신청됨** <sub>19:43 · `b11c3bf`</sub>
  내가 문서에 "아직 안 함"이라고 적어두고, 이후 그 문서를 근거로 계속 같은 말을 반복했다. 실제 신청 내역을 확인했어야 했다.
- **CLAUDE.md 갱신: 오늘 작업 정리** <sub>19:11 · `5ebe6c9`</sub>
  - 현재 상태를 실제 수치로 (소개글 국문 2,994 / 영문 2,972, 매일 자동 수집 중) - 다음 세션 지침을 "승인 메일 확인부터"로 교체. 승인별 할 일 표 - 신청 현황 갱신 (YouTube 08-12 제출 완료) - YouTube 심사 제출물 목록 + 재감사 대비 주의사항   (증빙에 주소 표시줄 필수 / 폼 새로고침 금지 / GCP 프로젝트 번호) - 명령어 절 신설. 가공 스크립트는 순서를 지켜야 한다   (load:raw -> dedup -> build:places -> build:tags) - .bat ASCII 제약 기록 — 성공한 것처럼 보이는 실패라 안 적으면 또 당한다 - 채널 분석 실데이터 연결 + 실측/시연이 한 화면에 섞인다는 경고
- **5단계 검색어 재설계: 여행 맥락 + 지역 우선** <sub>19:04 · `9be8bdf`</sub>
  실측 결과(eval:videoplace)를 반영했다. 소재 이름만으로 검색하면 여행 영상이 아니라 먹방·일상 영상이 오고, 그 채널들은 장소 적중률 0% 였다.
- **영상->장소 연결 실측: 지역 단위로 설계를 바꿔야 한다** <sub>18:58 · `b3103ea`</sub>
  가장 검증 안 된 가정을 실제 영상 499편으로 확인했다. 쿼터 상향을 받고 나서 알면 늦는 종류의 문제였다.
- **매일 소개글 수집 자동화 (Windows 작업 스케줄러)** <sub>18:51 · `d32259b`</sub>
  한도가 오퍼레이션당 하루 1,000건이라 안 돌린 날은 그날치가 사라진다. 승인이 늦어져도 매일 2,000건은 쌓이게 매일 09:30 에 돌린다.
- **채널 분석 실데이터 연결 (S1)** <sub>18:44 · `4378a3c`</sub>
  화면에서 YouTube API 를 부르지 않는다 (SPEC 6장 절대원칙 1). 배포된 사이트는 우리 SQLite 도 못 읽는다 -> export:channels 로 빌드 시점 JSON 을 굽는다. 방문자가 늘어도 쿼터는 0 이다.
- **인쇄용 스타일: 방침·약관 PDF 가 안 읽히던 문제** <sub>15:36 · `06bcdab`</sub>
  화면은 --color-ground 가 #000 인 다크 전용이다. 브라우저는 인쇄할 때 배경을 기본으로 빼기 때문에, 그대로 PDF 로 뽑으면 흰 종이에 밝은 회색 글씨만 남아 아무것도 안 읽힌다. YouTube 심사에 약관 PDF 를 내려다 발견했다.
- **YouTube 심사 증빙: 홈 푸터 브랜딩 + 실제 임베드 한 곳** <sub>15:28 · `92257d5`</sub>
  심사가 요구하는 스크린샷 두 장을 만들 수 없는 상태였다. - "홈페이지에 YouTube 브랜딩과 개인정보처리방침 링크가 함께 보이는 화면"   -> 홈 푸터에 YouTube 로고·귀속 문구·약관/Google 방침 링크를 링크 줄 바로 아래 배치.      한 장에 같이 담겨야 해서 같은 푸터 안에 둔다. 영상 통계를 쓰는 이상 약관상으로도 필요. - "플레이어/삽입 스크린샷"   -> 추천 화면 영상 카드는 시연 데이터라 DEMO_ 자리표시만 그려진다.      /data-sources 에 실제 영상 한 편을 실측값 그대로 임베드했다.
- **신청 체크리스트: 무엇을 어디서, 지금 어떤 상태인지만** <sub>14:37 · `0c6a348`</sub>
  문안(docs/신청서-문안.md)과 분리했다. 신청할 때마다 문안 전체를 훑을 필요가 없다.
- **5단계 YouTube 수집기: 채널 분석에 search 를 쓰지 않는다** <sub>14:36 · `e0071d7`</sub>
  search.list 가 100 units, 나머지는 대부분 1 unit 이다. 채널 최근 영상을 search 로 찾으면 100, 업로드 재생목록을 타면 3 units. 실측으로 영상 50편에 3 units 였다 -> 기본 쿼터로도 채널 3,300개를 돌린다. 채널 분석 기능은 쿼터 상향을 기다릴 필요가 없다.
- **4단계 준비: 정답지로 태깅 정확도 측정 (96.7%)** <sub>14:16 · `12f0ada`</sub>
  소개글과 cat3 를 둘 다 가진 627건이 정답지다. 전량 돌리기 전에 여기서 프롬프트를 확정한다. 검증 없이 4만 건에 돌리면 틀린 태깅을 나중에 발견한다.
- **장소 마스터 승격 + 영문/국문 수집 충돌 수정** <sub>14:06 · `95930b9`</sub>
  폐교 334곳이 TourAPI 에 없어 태그를 붙일 대상 자체가 없었다. 주력 소재인데 목록에 없으면 추천이 안 된다.
- **3단계 태그 체계: 관광공사 분류를 그대로 쓴다** <sub>13:57 · `6615b67`</sub>
  설계하려고 데이터부터 봤더니 관광공사 분류가 이미 대분류 24 · 세부 153 이었다. SPEC 이 잡았던 "대분류 20 + 세부 150" 과 사실상 같고 5일장·등대·상설시장이 전부 소분류에 있다. 새로 만들면 두 분류 사이 대응표를 평생 관리해야 하고 그 표가 틀어지면 점수가 조용히 어긋난다.
- **YouTube API 심사 전제조건: 방침·약관·데이터 출처 페이지** <sub>11:24 · `ae746cf`</sub>
  감사 탈락 사유 두 가지를 막는다. - "방침 미비" -> /privacy. YouTube API Services 이용 고지, YouTube 약관·   Google 방침 링크, 권한 철회 안내를 넣었다 (심사 필수 항목) - "라이브 제품이 설명과 다름" -> /data-sources. 무슨 데이터로 무엇을   계산하는지, 하지 않는 것이 무엇인지 화면에 둔다
- **2단계-B 중복 정리: 지우지 않고 place_link 로 잇는다** <sub>11:05 · `5e8f3b1`</sub>
  정선 5일장은 TourAPI 와 전통시장 표준데이터 양쪽에 있다. 어느 쪽도 버릴 수 없어 (소개글 vs 장날·점포수) 짝만 남긴다.
- **1단계 수집 + 2단계 보강파일 적재 + 공유 데이터셋** <sub>10:55 · `278ee92`</sub>
  수집 스크립트 - collect-list / collect-overview / collect-extra: 콘텐츠 타입으로 나눠 돌린다   (areaCode 로 나누면 지역코드 빈 항목 26,631건이 통째로 빠진다) - tourapi.ts: 게이트웨이 오류 봉투(OpenAPI_ServiceResponse)를 따로 읽는다   이걸 안 봐서 쿼터 초과를 못 잡고 18,119건을 날렸었다 - load-raw: 전통시장 1,393 / 폐교 1,194 / 철도역 215 적재   시장개설주기가 "2일+7일" 로 들어와 장날을 LLM 없이 확보

## 2026-08-04  <sub>10:58~16:32 · 11건</sub>

- **CLAUDE.md 갱신: 다음 작업(정보 카드 재구성) + 화면 구조/문구 원칙 기록** <sub>16:32 · `c9f16b3`</sub>
- **공항 3곳(인천/김해/제주)에서 최근접 노선, 소재 현황 블록으로 압축, 예상 도달은 소재 단위** <sub>16:30 · `cf6ad61`</sub>
- **플로우 정리: 옛 경로 홈으로 통합, 소재 전환 고정바, 항공 노선형 지도, 문구 전면 수정** <sub>16:17 · `af48dae`</sub>
- **홈 단일 플로우: 채널 입력 -> 히어로에 분석 -> 아래 추천, 카카오 공유 추가** <sub>16:08 · `aa13c48`</sub>
- **실행.bat 추가 - 더블클릭으로 서버 실행 + 브라우저 자동 열기** <sub>15:58 · `80b0c85`</sub>
- **S4 재배치: 사진 먼저, 운영정보 머리말로, 설명 문구 제거** <sub>15:56 · `81c06fb`</sub>
- **빈 자리를 문장으로 채우지 않는다 - 설득 문구/추정 고지 제거** <sub>14:48 · `232889a`</sub>
- **태그를 6개 축으로 확장 + 지도 축척 버그 수정** <sub>14:40 · `9eaa3f4`</sub>
- **홈을 크리에이터용 서비스 화면으로 재작성 - 히어로에 입력창, 기능은 실제 결과로 제시** <sub>11:35 · `eec71a9`</sub>
- **홈을 스크롤형 랜딩으로 재작성 + 히어로 지도 노출 개선** <sub>11:14 · `5b5b974`</sub>
- **디자인 적용: 검정+파랑+금색 토큰, 도트 지도 히어로, 공식 노출** <sub>10:58 · `1cf9b9b`</sub>

## 2026-08-03  <sub>15:55~20:53 · 4건</sub>

- **TourAPI 9개 서비스 전부 연동 확인 - 오퍼레이션명/필수 파라미터 확정** <sub>20:53 · `82ad066`</sub>
- **TourAPI 실측 진단 + 태그 샘플 수집 (소개글 확보율 99%)** <sub>18:41 · `311f66b`</sub>
- **0단계: .env 견본 정비 — 발급받은 관광공사 9종 + 추가 필요 목록 정리** <sub>17:16 · `4593cb8`</sub>
- **0단계 D: S5 어드민 콘솔 — 0단계 완료** <sub>15:55 · `9c7c2cb`</sub>
  - 성과 환산 4종 + 성공 지표(인구감소지역 신규 영상 수)를 별도 강조   추천 건수가 아니라 콘텐츠가 실제로 생겼는지가 유일한 성공 지표다 - 시군구별 미개척 소재 랭킹 (막대 + 대표 소재 + 해외 성과) - 채널 <-> 지역 매칭. 섭외 근거를 함께 표시 - 추천 로그는 기록만. "비슷한 채널이 많이 고른 소재"의 재료

## 2026-08-02  <sub>16:49~21:37 · 3건</sub>

- **0단계 C: S4 상세 + 장소 데이터 보강 (태그별 성적, 촬영 컷, 장날, 근처 소재)** <sub>21:37 · `f3bbc30`</sub>
- **0단계 B: 추천 흐름 재설계 + S1~S3 화면** <sub>21:00 · `8866ae4`</sub>
  크리에이터는 "아무도 안 갔다"를 기회가 아니라 잘 될 증거가 없다는 신호로 읽는다. 실제로는 잘 된 영상을 참고해 똑같이 따라 만든다. 그래서 화면 구성을 뒤집었다.
- **0단계 A: 프로젝트 뼈대 + 타입 + 가짜 데이터 30건** <sub>16:49 · `aa3311e`</sub>
  - Next.js 15 + TypeScript + Tailwind v4 수동 구성 (create-next-app 미사용) - .env.example / .gitignore 선반영 (키는 파일에 직접 쓰지 않는다) - src/lib/types.ts      SPEC 3장 데이터 모델. snake_case 유지 → 7단계에서 DB 컬럼과 1:1 - src/lib/score.ts      SPEC 4장 계산식 (VSR, 폴백 규칙, 희소성, 예상 도달 범위) - src/lib/display.ts    SPEC 9장 표시 원칙 (미개척/표본부족/폴백/저신뢰/계절) - src/lib/i18n.ts       문구 단일 소스. ko 만 채우고 en 은 폴백 구조만 - src/lib/repo.ts       데이터 접근층. 7단계에서 바꾸는 유일한 파일 - src/data/fake/*       장소 30건 + 태그 49개 + 점수판. 예외 케이스 10종을 의도적으로 배치 - /check                육안 검증표. 예외 케이스 10종 자동 점검 + 상위 5곳 순위
